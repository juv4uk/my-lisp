name: Verification-only #305 narrate outcome full package

on:
  pull_request:

jobs:
  full-package:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - name: Full my-lisp package
        run: cargo test -p my-lisp
