name: machine atoms focused

on:
  push:
    branches: [feat/177-typed-operands-mainline]
  workflow_dispatch:

jobs:
  machine-atoms:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test -p my-lisp --test machine_atoms --test machine_typed_operands
