# Rust build artifacts
/target
**/target/

# wasm-pack build output (generated by the release workflow)
# Вихід wasm-pack (генерується release-workflow)
# wasm-pack-Build-Ausgabe (vom Release-Workflow erzeugt)
/public/wasm

# Node (benchmark/build scripts)
node_modules

# Local editor/OS files
.DS_Store
.vscode/
.idea/

# Private local files (not part of the language repo)
# Приватні локальні файли (не частина репозиторію мови)
# Private lokale Dateien (kein Teil des Sprach-Repos)
/private/

# Standalone Web HTML (generated artifact)
# Standalone Web HTML (згенерований артефакт)
# Standalone Web HTML (generiertes Artefakt)
/my-lisp-cli-web.html

# Local swarm-node runtime data (event journal, node identity) when run
# with a relative --data-dir from the repo root -- never source-controlled.
/.swarm-node/
.git-credentials

# Build artifacts
my-lisp-bin
span

# Racket compiled bytecode
**/compiled/

# Local scratch files
scratch/
