# План: виділення Rust+Lisp в окремий репозиторій

Робочий план для нової чат-сесії Claude Code, запущеної в `C:\Users\juv4u\Documents\my-lisp` (git worktree гілки `my-lisp`, вже запушений як публічний [github.com/juv4uk/my-lisp](https://github.com/juv4uk/my-lisp)). Прочитай спершу `CLAUDE.md` — там принципи мови й підтверджені рішення. Цей файл — конкретні кроки, не філософія.

**Поточний стан (оновлено):** Кроки 1–7 фактично вже виконані — репозиторій `my-lisp` виділений через `git filter-repo`, `Cargo.toml` звужений до 4 крейтів (`my-lisp`, `-cli`, `-wasm`, `-literate`), `src-tauri`/`src-cljs`/CLJS-тулінг відсутні, README/versioning.md переписані під мову, реліз-теги свої (`l*`-префікс). Не зроблено: Крок 8 — `github.com/juv4uk/my-idea` досі містить власну копію крейтів my-lisp і **не посилається** на цей виділений репозиторій як на залежність (перевірено на GitHub 2026-08-08).

## Крок 0 — узгодити з користувачем перед стартом

Не виконувати нижче нічого без явного підтвердження користувача на кожен великий крок (видалення файлів, переписування історії, публічний push). План — не мандат діяти автономно.

## Крок 1 — інструментарій для чистої історії

- Перевірити `git filter-repo --version`. Якщо відсутній — треба Python 3 (зараз недоступний у PATH, `python3`/`python` не знайдено) + `pip install git-filter-repo`, або `scoop install git-filter-repo` (у користувача вже є scoop у PATH).
- Без `git-filter-repo` можна зробити грубіше: залишити тільки потрібні файли одним великим комітом (втратити per-file історію для решти), або спробувати `git filter-branch` (повільний, вбудований, але офіційно deprecated).
- **Рекомендація:** дочекатись `git-filter-repo`, не робити "грубе" видалення, якщо користувач хоче зберегти історію crates/my-lisp.

## Крок 2 — визначити periметр (що лишається)

Ймовірний список того, що належить Rust+Lisp репо:
- `crates/my-lisp/` — ядро
- `crates/my-lisp-cli/` — CLI/REPL бінарник
- `crates/my-lisp-wasm/` — WASM-біндінги
- `crates/my-lisp-literate/` — literate-Markdown підтримка
- `lib/core.my` — bootstrap-бібліотека
- `Cargo.toml` / `Cargo.lock` (workspace root) — **потребує переписування**: зараз описує весь my-idea workspace (включно з `src-tauri`), треба звузити до чотирьох my-lisp-крейтів
- `docs/language-core.md`, `docs/quote-tutorial.md`, `docs/testing.md` (тільки Rust-частина), `docs/versioning.md` (адаптувати — версіонування тут своє, не успадковане від my-ide)
- `crates/my-lisp/tests/fixtures/conformance.json` (якщо є — перевірити шлях) і сам `crates/my-lisp/tests/mccarthy.rs`
- `benchmarks/*.my` + `scripts/benchmark.mjs` + `crates/my-lisp/examples/benchmark.rs` (якщо бенчмарки залишаються тут)
- `public/my-lisp-cli-web.html` + `scripts/make-portable-web.mjs` (якщо хочемо тримати веб-REPL демо в цьому репо, а не в my-idea)
- `LICENSE` (MIT, скопіювати як є)
- Корінний `README.md` — **переписати з нуля** під мову, не під IDE (поточний README — про my-idea)
- `CLAUDE.md` — вже є, оновлювати по ходу
- Новий/адаптований `.gitignore` (звузити з my-idea-версії — прибрати Android/Tauri/CLJS-специфічні рядки)

**Явно НЕ лишається:** `src-cljs/`, `src-tauri/`, `public/` (крім вищезгаданого HTML), `package.json`/`package-lock.json`/npm-тулінг, `shadow-cljs.edn`, `app-icon.svg`, `.github/workflows/*` (крім, можливо, адаптованого CI для Rust-тестів), `docs/android-release.md`, `docs/platform-roadmap.md`, `docs/release-assets.md`, `docs/windows-arm64.md`, `docs/source-files.md` (IDE-специфічний), `docs/benchmarks.md` (якщо бенчмарки не переносяться), `scripts/release.ps1`/`release.sh` (IDE-релізний процес), `scripts/setup-android-signing.ps1`, `scripts/configure-android-signing.mjs`, `scripts/verify-windows-architecture.ps1`, `test_parse.rs` (старий debug-скрипт для перевірки `pulldown_cmark`-парсингу literate-блоків, закомічений у `8c16ea1`; не частина жодного крейта — ймовірно можна викинути, а не переносити).

**Обговорити з користувачем окремо:** чи `crates/my-lisp-wasm` і веб-REPL демо лишаються тут, чи в my-idea (там вони теж використовуються для Language Lab — можливе дублювання коду між двома репо, якщо не виділити спільний крейт).

## Крок 3 — виконати вирізання історії

Коли периметр узгоджено і `git-filter-repo` встановлено:

```bash
git filter-repo --path crates/my-lisp --path crates/my-lisp-cli \
  --path crates/my-lisp-wasm --path crates/my-lisp-literate \
  --path lib/core.my --path LICENSE \
  --path docs/language-core.md --path docs/quote-tutorial.md \
  --path docs/testing.md --path CLAUDE.md \
  ... # інші шляхи з Кроку 2
```

Робити це **в самій `C:\Users\juv4u\Documents\my-lisp` папці** (вона вже ізольований worktree/клон гілки, не основний `my-idea`), щоб не зачепити `my-idea`. `git-filter-repo` за замовчуванням переписує весь локальний репозиторій, тому перед запуском варто ще раз перевірити, що `git remote -v` тут показує тільки `my-lisp`/`origin` на `github.com/juv4uk/my-lisp`, а не залежить від `my-idea`.

## Крок 4 — новий Cargo workspace root

Переписати корінний `Cargo.toml`:
```toml
[workspace]
members = ["crates/my-lisp", "crates/my-lisp-cli", "crates/my-lisp-wasm", "crates/my-lisp-literate"]
resolver = "2"
```
Прибрати `src-tauri` з членів workspace. Перевірити, чи є root-level release-профіль (`opt-level = "z"`, `lto = true` — згадувалось у `crates/my-lisp-wasm/Cargo.toml` як "визначено в корені workspace") — перенести його сюди без змін.

## Крок 5 — новий README

Написати з нуля, трилінгвально (EN/UA/DE, конвенція проєкту), на основі змісту `crates/my-lisp/README.md` і `docs/language-core.md`, але як головний вхідний документ репо:
- "A small language that grows itself"
- Швидкий старт: `cargo run -p my-lisp-cli`, або скачати `my-lisp-cli-web.html` без встановлення
- Посилання на `docs/quote-tutorial.md`
- McCarthy-контракт, точна раціональна арифметика — коротко, з посиланням на `docs/language-core.md`
- Тести: `cargo test --workspace`

## Крок 6 — CI

Новий/адаптований `.github/workflows/ci.yml`: `cargo test` для чотирьох крейтів (як зараз у my-idea, тільки без npm/CLJS кроків). Окремо подумати, чи потрібен release-workflow тут (публікація `my-lisp-cli` бінарників для кількох ОС — уже є робочий приклад у `my-idea`'s `cli-release.yml`, можна адаптувати).

## Крок 7 — версіонування

`crates/my-lisp` вже має власну версію `0.1.0` (незалежну від my-idea `0.x.x`, за `docs/versioning.md`). Вирішити з користувачем: чи цей репо стартує тегами з `v0.1.0`, чи з чогось іншого — не вигадувати самому.

## Крок 8 — зв'язок з my-idea

Після того як новий репо стабілізується, повернутись до `my-idea` і вирішити (окреме обговорення з користувачем, не автоматично):
- чи `my-idea` тепер тягне my-lisp як git submodule / залежність з crates.io / просто копіює на реліз,
- чи `crates/my-lisp*` в `my-idea` видаляються повністю,
- як синхронізувати виправлення (баг в evaluator має чинитись в одному місці, не в двох копіях).

## Крок 9 — напрямки розвитку мови, узгоджені з духом Маккарті

Не мандат діяти автономно — як і решта плану, кожен пункт узгоджувати з користувачем перед стартом. Пріоритет — зверху вниз.

1. ✅ **Метациркулярний evaluator самою my-lisp.** Зроблено: [`lib/meta-eval.my`](../lib/meta-eval.my) (окремий файл, не частина завжди завантажуваного `lib/core.my`), 9 тестів у `crates/my-lisp/tests/meta_eval.rs`, короткий приклад у `docs/quote-tutorial.md` (Етап 6). `(my-eval expr env)` інтерпретує quoted-списки через `read`/`car`/`cdr`/`cond`/`lambda`/`eq`/`atom`, за схемою eval/apply Маккарті 1960 року — примітиви диспетчеризуються напряму, не переписані заново.
2. ✅ **`tests/fixtures/conformance.json` — тримати як єдину точку правди**, коли з'явиться C-ядро чи HDL-реалізація (див. CLAUDE.md, "Confirmed future direction"). Зроблено: [`tests/fixtures/README.md`](../tests/fixtures/README.md) — формат, правила (спільна сесія між фікстурами, незмінність опублікованих значень), і новий тип фікстур — `{ "expr": "...", "error": "ErrorKind" }` для очікуваних помилок, не лише успішних обчислень (раніше контракт не перевіряв взагалі, чи падає щось правильним видом помилки). Не давати нових реалізацій ставати "ще одним несумісним діалектом" — саме ця фрагментація Lisp-екосистеми 70-80х засмучувала Маккарті найбільше.
3. ✅ **Невеликий символьний AI-приклад: машина логічного висновування (Advice Taker).** Зроблено: [`lib/unify.my`](../lib/unify.my) — unification (з повноцінним occurs-check) та [`lib/reason.my`](../lib/reason.my) — backward-chaining Prolog-подібний рушій (зі standardizing apart для підтримки рекурсивних правил). Логічні змінні представлені як `(var name)`-пари, а не голі символи (`eq` вимагає атомів, але ми навчили уніфікатор працювати з `equal?` для імен). Тести у `crates/my-lisp/tests/unify.rs` та `crates/my-lisp/tests/reason.rs`. Це наблизило my-lisp до первісного бачення Джона Маккарті (програмування як навчання системи фактам).
4. ✅ **Пояснюваність (explainability).** Зроблено: `explain-proof` у [`lib/reason.my`](../lib/reason.my) будує дерево доведення (`proved`/`proved-not` вузли) і друкує людський трейс — "чому" система дійшла висновку, не лише "що". Тест `test_explain_proof` у `crates/my-lisp/tests/reason.rs`.
5. ✅ **Модульність знань (knowledge packages).** Зроблено: [`lib/knowledge.my`](../lib/knowledge.my) — `defmodule`, ізольовані запити через `reason-in`, і `tell-knowledge` з перевіркою конфліктів (доводить заперечення нового факту перед додаванням). Відповідає пункту "знання — пакети, які можна завантажувати, перевіряти, комбінувати" з `private/lisp-to-knowledge.md` §10.
6. ❌ **NLP-міст (`understand`), перша версія — видалено.** Була зроблена (`crates/my-lisp-cli/src/llm.rs` + `:tell`/`:ask`), але лишалась REPL-хаком: єдине місце в кодбазі без тестів, без trilingual-конвенції, з мережевою залежністю та зовнішнім API-ключем — не на рівні строгості решти проєкту. Видалено разом з `reqwest`/`serde`/`serde_json` з `crates/my-lisp-cli/Cargo.toml`. Якщо повертатись до цього кроку (трійка `eval`/`reason`/`understand` з `private/lisp-to-knowledge.md` §9) — робити одразу з тестами й документацією, а не потім.
7. ✅ **Провенанс тверджень (statement provenance) — перший крок.** Зроблено: `reason-explain` у [`lib/reason.my`](../lib/reason.my) явно розрізняє "доведено" (пояснює перше доведення через `explain-proof`) від "не можу довести" (прямо каже про це замість мовчазного порожнього списку). Тести: `reason_explain_explains_a_provable_goal`, `reason_explain_says_so_when_a_goal_cannot_be_proved` у `crates/my-lisp/tests/reason.rs`. Проте-дерево вже саме по собі несе джерело (факт vs правило — видно з порожнього/непорожнього списку підцілей у вузлі `proved`), тож структуроване `(джерело ...) (впевненість ...)` з §12 залишається можливим наступним кроком, якщо знадобиться — поки що не потрібне.
8. ✅ **Атом як вхід у поняття (concept entry point).** Зроблено: `describe` у [`lib/knowledge.my`](../lib/knowledge.my) приймає символ і назву модуля, повертає всі факти цього модуля, що згадують символ (наприклад, `(describe 'earth 'astronomy)` → `((planet earth))`). Без нового примітиву — лягло на вже наявну модель фактів `defmodule`, лише новий напрям запиту (від символу назовні, а не від цілі вглиб, як `reason`). Тести: `test_describe_collects_every_fact_about_a_symbol`, `test_describe_unknown_module`, `test_describe_symbol_with_no_facts` у `crates/my-lisp/tests/knowledge.rs`.
9. ✅ **Лічильник використання (usage counting) — вимірювана "живість" дерева знань.** Зроблено: `count-usage`/`merge-usage` у [`lib/reason.my`](../lib/reason.my) обходять дерево доведення (`proved`-вузли) і повертають `(голова-правила . скільки-разів-використано)`. Справжня мутація (`(times-used N)` inline у факті, інкрементована всередині `prove-rule`) виявилась неможливою без нового `set!`-примітиву: `def` в my-lisp мутує лише поточний фрейм ([environment.rs:70](../crates/my-lisp/src/environment.rs:70)), а `prove-rule` виконується у вкладених `lambda`-викликах — тож обрано чисто функціональний підхід (рахувати з уже наявного дерева доведення) замість додавання мутабельного стану в рушій. Накопичення між запитами — `*usage-counts*`/`record-usage!`/`usage-of` у [`lib/knowledge.my`](../lib/knowledge.my), за тим самим top-level `def`-паттерном, що й `*knowledge-base*` (`record-usage!` **має** викликатись на верхньому рівні, не всередині `let` — інакше мовчки створює локальний binding, який зникає). Побічно виявлено й задокументовано баг reader'а: dotted-pair-літерали (`'(p . 0)`) не парсяться як справжні dotted pairs (`.` читається як звичайний символ), хоча printer саме так друкує реальні dotted pairs — див. `docs/testing.md`. Тести: `count_usage_counts_each_rule_head_that_contributed_to_a_proof`, `count_usage_sums_repeated_use_of_the_same_fact` у `crates/my-lisp/tests/reason.rs`; `test_record_usage_accumulates_across_separate_queries`, `test_usage_of_unrecorded_rule_is_zero` у `crates/my-lisp/tests/knowledge.rs`.
10. ✅ **Провенанс тверджень — повна структура.** Зроблено: `provenance`/`provenance-list` у [`lib/reason.my`](../lib/reason.my) перетворюють вузол дерева доведення на явний запис `(statement ціль (source fact|rule) (rule голова-правила) (derived-from список-підтверджень))`, рекурсивно для всього дерева. Свідомо без полів `(confidence ...)`/`(time ...)` з `private/lisp-to-knowledge.md` §12 — рушій точний і детермінований (той самий принцип точних чисел, що й в арифметиці), тож числова "впевненість" була б вигаданою точністю. Чиста надбудова над вже наявним деревом доведення (той самий підхід, що й `count-usage`), без нового примітиву. Тести: `provenance_marks_a_bare_fact_as_source_fact_with_no_derivation`, `provenance_marks_a_rule_application_as_source_rule_with_its_derivation` у `crates/my-lisp/tests/reason.rs`.
11. ✅ **NLP-міст, контрольована природна мова (без LLM).** Зроблено: [`lib/understand.my`](../lib/understand.my) — `understand` зіставляє фіксовані форми **списку слів** (не рядка — у my-lisp нема рядкових примітивів: split, доступ до символів; додавати їх лише заради токенізації означало б розширювати поверхню Rust built-in) зі знаннєвим clause: `(X is a Y)`/`(X is Y)` → факт `((Y X))`, `(X V Y)` → факт `((V X Y))`, `(all X have Y)` → правило `((has (var w) Y) (X (var w)))`. Без морфології (без відкидання множини, без відмінювання) — виклик уже подає точну однину класу. Результат напряму згодовується в `reason` без ручного редагування. Це перша половина мосту "текст → структура" з `private/lisp-to-knowledge.md` §6 — повний LLM-гібрид (друга половина, вільний текст) залишається окремим майбутнім кроком, і має відповідати тій самій строгості (тести, trilingual), що й тут. Тести: `understand_is_a_produces_a_class_membership_fact`, `understand_is_without_article_produces_the_same_fact`, `understand_subject_verb_object_produces_a_relation_fact`, `understand_all_have_produces_a_universal_rule`, `understand_output_is_usable_directly_as_a_reason_rule` у `crates/my-lisp/tests/understand.rs`.

12. 🔶 **Forward-chaining рушій у стилі CLIPS — Крок 1-7, 10-12, 14 з N.** Крок 1 зроблено: [`lib/forward.my`](../lib/forward.my) — `*working-memory*` (той самий top-level `def`-паттерн, що й `*knowledge-base*`/`*usage-counts*`), `assert-fact!` (макрос, той самий трюк, що й `record-usage!`/`tell-knowledge`), `fire-rule` — унікація шаблону одного правила з одним фактом і підстановка в template через `apply-subst`, або `'no-match` при невдачі. Крок 2 зроблено: `fire-rule-on-facts` — застосування одного правила до всього списку фактів, збір нових фактів, `'no-match` мовчки відкидається (guard `(atom result)` перед `eq`, той самий паттерн, що й `var?` у `lib/unify.my` — template може бути складеним списком, і `eq` впав би на не-атомі); `fire-rule-on-working-memory` — те саме напряму з `*working-memory*`. Крок 3 зроблено: `fire-rules-on-facts` — застосування **всього набору правил** до списку фактів за один прохід (перебір усіх правил через `append`, той самий fan-out паттерн, що й `prove-goal` у `lib/reason.my`); `fire-rules-on-working-memory` — те саме напряму з `*working-memory*`. Крок 4 зроблено: `run` — fixpoint-цикл, повторно застосовує весь набір правил, дедуплікуючи нові факти через `equal?` (`member?`/`append-new`), поки прохід не додає нічого нового. `run` — чиста функція, що повертає фінальний список фактів, а не мутує `*working-memory*` зсередини циклу — та сама причина, що й функціональний `count-usage`: `run` це `lambda`, а `def` мутує лише свій фрейм, тож тіло циклу не може стійко оновлювати глобальний стан по ітерації. Синхронізація назад — окремим макросом `assert-facts!`, викликаним на верхньому рівні: `(assert-facts! (run rules *working-memory*))`. Крок 5a зроблено: `retract-fact` — видаляє один факт (порівняння через `equal?`) зі списку; `retract-fact!` — те саме напряму в `*working-memory*`. Свідомо простий retract, без truth maintenance: якщо факт X виведено з факту Y, і Y видаляють, X лишається — `run` не пам'ятає, які факти обґрунтовували які (продемонстровано тестом `retract_fact_bang_does_not_undo_facts_it_already_helped_derive`, навмисно, не як прихований баг). Крок 5b зроблено: справжня (хоч і мінімальна) TMS — *факт з обґрунтуванням* `(факт . підтримка)`, окрема глобальна `*justified-memory*` (не чіпає `*working-memory*`/Крок 1-5a). `assert-fact-tms!` — аксіома (порожня підтримка); `run-tms`/`run-tms!` — той самий fixpoint, що й `run`, але зберігає, який факт яку підтримку дав; `retract-fact-tms`/`retract-fact-tms!` — видалення факту **каскадує** на все, чия відстежена підтримка його називала (рекурсивно), а незалежно обґрунтовані факти лишаються (перевірено тестом `retract_fact_tms_bang_leaves_independently_supported_facts_alone`). Свідоме обмеження, задокументоване, не приховане: **одне обґрунтування на факт** — якщо факт можна незалежно вивести двома шляхами, видалення відстеженої підтримки все одно видалить факт, навіть якщо інше обґрунтування могло б зберегти його живим у повному JTMS; розширення до кількох обґрунтувань — окремий подальший крок, тут не мається на увазі. Крок 5c зроблено (у дусі Маккарті — не лишати задокументоване обмеження просто лежати, а закрити його чисто): справжній JTMS із **множинними** обґрунтуваннями. Запис — `(факт обґрунтування-1 обґрунтування-2 ...)`, окрема глобальна `*jtms-memory*` (не чіпає `*working-memory*`/Крок 1-5a чи `*justified-memory*`/Крок 5b). `assert-fact-jtms!` — аксіома з порожнім обґрунтуванням `()`; `run-jtms`/`run-jtms!` — fixpoint, що звіряє результат через `equal?`, а не підрахунок записів (Крок 4/5b рахували довжину — тут кількість записів може не змінитись, а обґрунтування додатись, тож підрахунок пропустив би реальний прогрес); `retract-fact-jtms`/`retract-fact-jtms!` — видаляє факт напряму, обрізає обґрунтування деінде, що його називали, і каскадує далі лише для записів, чий список обґрунтувань спорожнів повністю. Факт із живим незалежним обґрунтуванням (напр. одночасно аксіома й похідне) переживає видалення однієї підтримки — перевірено тестом `retract_fact_jtms_bang_keeps_a_fact_with_a_surviving_independent_justification`. Тести Кроку 1-5a: `fire_rule_produces_a_new_fact_when_the_pattern_matches`, `fire_rule_returns_no_match_when_the_pattern_fails`, `fire_rule_on_facts_collects_new_facts_and_drops_non_matches`, `fire_rule_on_facts_returns_empty_list_when_nothing_matches`, `fire_rule_on_working_memory_reads_the_global_fact_list`, `fire_rules_on_facts_applies_every_rule_and_collects_all_results`, `fire_rules_on_working_memory_reads_the_global_fact_list`, `run_reaches_a_fixpoint_by_chaining_rules_across_passes`, `run_does_not_loop_forever_when_a_rule_reproduces_an_existing_fact`, `assert_facts_merges_run_results_into_the_global_working_memory`, `retract_fact_removes_a_matching_fact_from_a_list`, `retract_fact_leaves_the_list_unchanged_when_nothing_matches`, `retract_fact_bang_removes_from_the_global_working_memory`, `retract_fact_bang_does_not_undo_facts_it_already_helped_derive`, `assert_fact_adds_to_the_global_working_memory`. Тести Кроку 5b: `assert_fact_tms_stores_an_axiom_with_no_support`, `run_tms_bang_derives_a_fact_with_its_support_recorded`, `retract_fact_tms_bang_cascades_to_everything_it_supported`, `retract_fact_tms_bang_leaves_independently_supported_facts_alone`. Тести Кроку 5c: `assert_fact_jtms_stores_an_axiom_with_an_empty_justification`, `run_jtms_bang_records_the_derivation_as_a_justification`, `retract_fact_jtms_bang_cascades_when_the_only_justification_is_gone`, `retract_fact_jtms_bang_keeps_a_fact_with_a_surviving_independent_justification`. Крок 6 зроблено (спільна мова правил з `reason.my` — друга з двох "насуплень Маккарті"): `fire-rule-multi`/`fire-rules-multi`/`run-multi` приймають правила точно у формі `(head cond1 cond2 ...)` з `lib/reason.my`, не форму `(pattern template)` Кроків 1-5. Справжня кон'юнктивна логіка зіставлення (`match-conditions`), що протягує підстановку через список умов проти явного списку фактів — той самий підхід, що й `prove-goals` у `reason.my`, лише без рекурсії в подальші правила. Доведено буквально тим самим правилом `grandparent`/`parent`, яким користується `rule_with_condition_backward_chaining` у `tests/reason.rs` — спрацьовує і вперед, без жодної зміни літералу (тест `run_multi_derives_the_same_conclusion_reason_would_from_the_same_rule_literal`). Свідомо адитивно: третій шар над пласким списком фактів, не чіпає й не замінює Кроки 1-5a/5b/5c. Тести Кроку 6: `fire_rule_multi_requires_every_condition_to_match_before_firing`, `fire_rule_multi_produces_nothing_when_one_condition_has_no_supporting_fact`, `run_multi_derives_the_same_conclusion_reason_would_from_the_same_rule_literal` — усе у `crates/my-lisp/tests/forward.rs`.

Крок 7 зроблено — прямо продовжуючи те, на що Крок 5 `lib/clips-import.my` вказав як на прогалину: **negation as failure** для forward-chaining. `match-one-condition`/`match-negated-condition` — умова `(not (шаблон))` успішна точно тоді, коли внутрішній шаблон *не вдається* зіставити з поточними фактами (та сама ідея, що й `prove-goal-state` у `lib/reason.my` для backward-chaining, лише без відстеження доведення). `match-conditions` тепер викликає `match-one-condition` замість `match-condition-against-facts` напряму. Доведено тим самим прикладом птах/пінгвін, що й `negation_as_failure` у `tests/reason.rs`. Після цього прибрано guard у Кроці 5 `lib/clips-import.my` (він більше не потрібен — `(not ...)` тепер імпортується й коректно працює); історія Кроку 5 лишена в коментарі, не стерта тихо. Тести: `run_multi_supports_negation_as_failure`, `match_negated_condition_fails_when_the_inner_pattern_matches`, `match_negated_condition_succeeds_when_the_inner_pattern_does_not_match` у `crates/my-lisp/tests/forward.rs`; `clips_defrule_with_a_not_condition_imports_and_runs_correctly` (замінив старий тест на пропуск) у `crates/my-lisp/tests/clips_import.rs`.

Крок 10 зроблено — той самий клас бага, що й `not` до Кроку 7, знайдений тим самим методом (перевірка, не здогадка): правило з CLIPS `(or (cat ?x) (dog ?x))` не виводило нічого (мало дати `(pet tom)`/`(pet rex)`), бо жоден факт ніколи не має головою символ `or`. `condition-is-or?`/`match-or-condition` — `or` успішний з **об'єднанням** збігів кожної альтернативи, кожна альтернатива пробується рекурсивно через `match-one-condition` (тож альтернатива сама може бути `not` чи вкладеним `or`). На відміну від Кроку 9 (де `clips-import.my` довелось патчити окремо), тут виправлення в самому `lib/forward.my` автоматично покриває й CLIPS-імпортовані правила з `or` — жодних змін у `clips-import.my` не знадобилось. Тести: `run_multi_supports_or_conditions`, `match_or_condition_unions_matches_from_every_alternative`, `match_or_condition_returns_nothing_when_no_alternative_matches` у `crates/my-lisp/tests/forward.rs`.

Крок 11 зроблено — той самий клас бага знову, знайдений тим самим методом: `(or (and (cat ?x) (small ?x)) (dog ?x))` узагалі не зіставляла гілку `and` (мало дати `(match tom)` теж, дало лише `(match rex)`), бо жоден факт теж ніколи не має головою `and`. `match-and-condition` — не нова реалізація, а прямо `match-conditions` сам, перевикористаний під іменем, якого чекає диспетчеризація за позицією умови (умови верхнього рівня вже й так неявна кон'юнкція — внутрішній `and` та сама операція, лише явно записана, найчастіше щоб згрупувати умову всередині `or`). Тести: `run_multi_supports_and_conditions_nested_inside_or`, `match_and_condition_requires_every_sub_condition_to_match`, `match_and_condition_fails_when_one_sub_condition_has_no_match` у `crates/my-lisp/tests/forward.rs`.

Крок 12 зроблено — четвертий раз той самий клас бага, знову знайдений перевіркою перед фіксом: `(test (> ?x 5))` не зіставляла нічого (мало дати `(big 10)`, дало нічого), бо жоден факт теж ніколи не має головою `test`. На відміну від `not`/`or`/`and`, `test` — не комбінатор зіставлення, а обчислення довільного виразу: `match-test-condition` спершу `apply-subst` розв'язує кожен `(var name)` у виразі проти поточної підстановки, тоді `eval` виконує вже повністю розв'язаний вираз як звичайний my-lisp-код (той самий read/eval-примітив, що скрізь у проєкті). Правдивий результат — успіх з незмінною `subst` (без нових зв'язувань, як і `not`), інше — провал. Тести: `run_multi_supports_test_conditions`, `match_test_condition_succeeds_when_the_expression_is_truthy`, `match_test_condition_fails_when_the_expression_is_falsy` у `crates/my-lisp/tests/forward.rs`.

Крок 14 зроблено (більший, узгоджений окремо архітектурний крок): багатоумовний близнюк JTMS. Перевірено перед стартом (не здогадано): `run-jtms!` із точно тим самим літералом `grandparent`/`parent`, що скрізь у тестах проєкту, не виводив нічого — `fire-rule-jtms` досі був побудований на однофактовому `fire-rule` Кроку 1, без жодного шляху до правила `(head cond1 cond2 ...)`. `match-conditions-jtms` — знову `thread-conjunction`, що протягує багатший стан `(підстановка . використані-факти)` замість голої підстановки — та сама зміна форми, яку `prove-goal-state` у `lib/reason.my` робить для `(підстановка доведення)`. Звичайна умова додає зіставлений факт до обґрунтування; `not`/`test` не додають нічого; `or` об'єднує обґрунтування кожної альтернативи; `and` — знову `match-conditions-jtms` над під-умовами (те саме перевикористання, що й не-JTMS `match-and-condition`). Свідомо адитивно — `fire-rule-jtms`/`run-jtms`/`run-jtms!` не чіпаються, лишаються чинними для форми `(pattern template)` Кроків 1-5; `run-jtms-multi!` — нова точка входу для спільного формату. Тести: `run_jtms_multi_derives_a_multi_condition_grandparent_fact`, `retract_fact_jtms_bang_cascades_through_a_multi_condition_derivation` у `crates/my-lisp/tests/forward.rs`. **Рефакторинг (одразу після Кроку 6, за прямою вказівкою "з'єднай forward і backward в один рушій", у дусі Маккарті — не форсувати штучну симетрію, а винести справжній спільний код):** `thread-conjunction`/`thread-conjunction-branches` — новий спільний примітив у [`lib/unify.my`](../lib/unify.my), єдине місце, де живе "протягнути стан крізь кон'юнкцію умов, розгалужуючись на кожній". `reason.my`'s `prove-goals` (тепер через `prove-goal-state`/`map-goal-results`, протягує `(підстановка . доведення)`) і `forward.my`'s `match-conditions` тепер обидва побудовані на ньому, замість двох окремих майже-однакових реалізацій обходу кон'юнкції. Різниця лишається тільки в `try-one` (рекурсивний пошук по правилах проти зіставлення з явним списком фактів) — саме та частина, яка й мала лишитись різною. Поведінково точна еквівалентність підтверджена: усі 153 наявні тести (включно з точними рядками дерев доведення в `explain-proof`/`provenance`/`count-usage`, і всіма TMS/JTMS-тестами) пройшли без жодної зміни очікуваних значень. Додано 2 прямі тести на сам `thread-conjunction`: `thread_conjunction_finds_every_combination_satisfying_all_conditions`, `thread_conjunction_returns_no_results_when_a_condition_cannot_be_satisfied` у `crates/my-lisp/tests/unify.rs`.

13. ✅ **Друга половина мосту — "структура → текст" (`narrate`).** Зроблено (наступний логічний тиск за принципом 5 з `private/mccarthy-principles.md`): [`lib/narrate.my`](../lib/narrate.my) — `narrate-fact` є точною структурною оберненою функцією до `understand-is`/`understand-relation` з [`lib/understand.my`](../lib/understand.my) (перевірено round-trip тестами: `understand` → `narrate-fact` повертає ті самі слова); `narrate-provenance` обходить запис `provenance` (з `lib/reason.my`) у пояснення-список слів, з'єднане `because`/`and`, наратуючи поле `rule` кожного вузла (завжди конкретне для листків), не поле `goal` (може нести нерозв'язані `(var name)` — та сама властивість, що вже є в `explain-proof`, задокументовано, не приховано). Виявлений і виправлений баг під час написання тестів: `append` у my-lisp приймає рівно 2 аргументи, виклик з трьома (`(append a b c)`) падав з помилкою арності — виправлено на вкладені `append`/`cons`. Тести: `narrate_fact_undoes_understand_is_a`, `narrate_fact_undoes_understand_relation`, `narrate_provenance_explains_a_bare_fact_with_no_because`, `narrate_provenance_explains_a_derived_fact_with_because_and_and`, `assert_understand_and_narrate_are_direct_inverses_for_the_is_a_shape`, `narrate_provenance_surfaces_unresolved_variables_in_a_derived_rule_head` (тест-запобіжник на задокументоване обмеження — доданий за прямою вказівкою після звірки з `private/mccarthy-principles.md` п.4/п.7: обмеження, назване словами, мусить мати тест, що не дасть йому тихо "зникнути" чи погіршитись непомітно) у `crates/my-lisp/tests/narrate.rs`.

14. ✅ **`forward-in` — інтеграція `knowledge/*.my`-модулів з forward-chaining.** Зроблено (обговорення історії CLIPS в NASA навело на думку перевірити, чи наш forward-chaining взагалі стикується з наявними знаннєвими модулями): [`lib/knowledge.my`](../lib/knowledge.my) отримав `forward-in` — аналог `reason-in`, але замість цілі "матеріалізує" все, що модуль може вивести, через `run-multi` з `lib/forward.my`, до fixpoint. Новий формат clause не знадобився: плаский список `defmodule` (факти й правила разом, той самий `(head cond1 cond2 ...)`) уже точно те, чого чекає `run-multi` — факт це просто "правило" з порожнім тілом умов, яке `match-conditions`/`thread-conjunction` тривіально задовольняють, тож воно застосовується безумовно (нешкідливе повторне виведення себе самого). `forward-in` стартує `run-multi` з порожнього списку фактів — власні факти модуля самі запускають fixpoint. Перевірено на обох наявних модулях: `knowledge/astronomy.my` (факти + одне правило `orbits`) і `knowledge/physics.my` (ланцюжок із двох правил, з яких одне ніколи не спрацьовує через відсутність `planet`-фактів у цьому модулі — теж правильно відображено в результаті). Тести: `test_forward_in_materializes_every_derivable_fact_in_a_module`, `test_forward_in_unknown_module`, `test_forward_in_chains_multiple_rules_in_one_module` у `crates/my-lisp/tests/knowledge.rs`.

15. 🔶 **Універсальний(-уватий) імпортер зі старих символьних AI-систем — Крок 1-5, 8-9, 13, 15-19 з N: CLIPS `deffacts`/`defrule`/multi-assert/файл з диска/`deftemplate`/`printout`/шаблони в `or`/`and`/багатофактовий `assert`/докстрінг+`declare`/`defmodule`-кваліфіковані імена+мультислот без значення/стек-безпечний імпорт великих файлів/`exists`+`forall`-guard (Крок 5 замінено Кроком 7 `lib/forward.my`: `not` тепер імпортується й працює коректно, guard прибрано).** Зроблено (за явним узгодженням: спершу CLIPS, спершу деффакти, спершу як knowledge/*.my-модулі): [`lib/clips-import.my`](../lib/clips-import.my) — `clips-import` читає справжні CLIPS-форми (`(deffacts name (факт1 ...) ...)`) як **дані**, не код, через власні `read`/`quote` my-lisp — жодного нового Rust-коду чи токенізатора не знадобилось, бо `?x`, `=>`, `defrule` та інші CLIPS-конструкції парсяться reader'ом як звичайні символи (перевірено напряму перед написанням коду). Конвертує в той самий формат clause без умов, якого чекає `defmodule` — результат напряму йде в `defmodule`/`reason-in`/`forward-in`, без ручного редагування. Непідтримувані форми (`defrule` — Крок 2, потребує конвертації CLIPS `?x` у `(var x)`) мовчки пропускаються, не валять увесь імпорт. Крок 2 зроблено: `defrule` з CLIPS-синтаксисом змінних `?x`. Наткнулись на реальну стіну (перевірено, не здогад): у my-lisp нема жодної інтроспекції символів — неможливо чистою my-lisp визначити "чи цей символ починається з `?`", щоб відрізати префікс і отримати чисте ім'я. Узгодили з користувачем: додати мінімальні Rust-примітиви ("ми не фанатики") замість штучного обходу. Додано 4 малих загальних примітиви в `crates/my-lisp/src/eval/special_forms.rs` (не один ad-hoc "strip-question-mark" хелпер): `symbol?` (предикат — потрібен, бо аргумент CLIPS-факту може бути числом, не лише символом, і `symbol->string` впав би на не-символі), `symbol->string`/`string->symbol` (конвертація в обидва боки), `string-first`/`string-rest` (аналоги `car`/`cdr` для рядка). `clips-convert-vars` рекурсивно обходить CLIPS-терм, замінюючи кожен `?x`-подібний символ на `(var x)` (ім'я змінної — саме power `?x` без `?`, не перейменоване й не namespace-нене, тож той самий `?x` двічі в правилі лишається тією самою логічною змінною). Підтримує рівно один висновок `(assert ...)` після `=>` (найпоширеніша форма CLIPS) — правила з кількома чи нульовою кількістю `assert` імпортуються як відсутність clause, за тією самою політикою "пропустити, не впасти", що й непідтримувані форми. По дорозі виправлено два реальні `eq`-баги арності/типу (виявлені тестами, не здогадані): `clips-split-at-arrow` падала на `(eq (car body) '=>)`, коли `(car body)` — складений список (умова на кшталт `(planet ?x)`), і `clips-defrule->clauses` падала на `(eq conclusion 'none)`, коли `conclusion` — реальний (складений) висновок; обидва виправлено тим самим guard-паттерном `(atom ...)` перед `eq`, що й скрізь у проєкті. Тести: `clips_deffacts_becomes_zero_condition_clauses`, `clips_import_skips_unsupported_forms_without_erroring`, `clips_import_result_feeds_straight_into_defmodule`, `clips_defrule_converts_question_mark_variables_to_var_terms`, `clips_defrule_leaves_non_variable_arguments_untouched`, `clips_defrule_with_multiple_conditions_converts_all_of_them`, `clips_defrule_with_no_or_multiple_asserts_imports_as_no_clauses`, `clips_import_mixes_deffacts_and_defrule_into_one_usable_module` у `crates/my-lisp/tests/clips_import.rs`; нові Rust-примітиви — `symbol_to_string_and_back_round_trips`, `string_first_returns_a_one_character_string`, `string_rest_drops_exactly_the_first_character`, `symbol_to_string_rejects_a_non_symbol`, `string_rest_rejects_an_empty_string` у `crates/my-lisp/tests/mccarthy.rs`. Крок 3 зроблено — за консультацією з `private/mccarthy-principles.md` (п.1/п.3: чиста my-lisp, без нового capability boundary, замість відкриття файлового I/O для читання `.clp` з диска, що лишається окремим майбутнім рішенням): будь-яка кількість `(assert ...)` після `=>`, не лише одна. N тверджень з однією LHS стають N окремими clause, по одному на кожне твердження, усі з тим самим списком умов. Досі обмежено лише `assert` — правило, чия `=>`-сторона містить іншу CLIPS-дію (`printout`, `retract`, `modify`), імпортується як відсутність clause, тією самою політикою "пропустити, не впасти". Тести: `clips_defrule_with_no_asserts_imports_as_no_clauses`, `clips_defrule_with_multiple_asserts_produces_one_clause_per_assert` у `crates/my-lisp/tests/clips_import.rs`.

Крок 8 зроблено (перед вибором обсягу — перевірка без здогадки: CLIPS-правило з `not`, імпортоване через `clips-import`, коректно доводить і через `reason-in` (backward), не лише `forward-in` — Крок 6 справді дав спільну мову для обох рушіїв, без бага): **`deftemplate`**, іменовані слоти CLIPS. Факти my-lisp лишаються чисто позиційними (нема причини розширювати представлення лише щоб віддзеркалити CLIPS) — оголошений порядок слотів шаблону стає фіксованим позиційним порядком, у який конвертуються факти: `(deftemplate reading (slot sensor) (slot value))` + `(reading (value 98) (sensor probe1))` → `(reading probe1 98)`, незалежно від порядку слотів у самому факті. Дво-прохідний, чисто функціональний підхід (`clips-templates-from-forms` сканує весь список форм наперед, будує alist `(назва . (слот1 слот2 ...))`) — не робочий мутабельний реєстр. Застосовується до конвертації термів **перед** конвертацією `?x`-змінних (порядок має значення: спершу репозиціювання за іменем слота, тоді підстановка `(var x)` для значень-змінних), і в `deffacts`, і в умовах/висновках `defrule` (з рекурсією крізь `not`-обгортку). Виявлено й підстраховано реальний крайовий випадок під час написання тестів: якщо позиційний факт випадково має ту саму назву, що й непов'язаний `deftemplate` (суперечливий, неправильно сформований CLIPS-вхід), `clips-lookup-slot-value` раніше падала на `car` не-пари; тепер деградує до "значення не знайдено" — той самий дух "пропустити, не впасти", що й у решті файлу. Тести: `clips_deftemplate_converts_named_slots_to_positional_order`, `clips_deftemplate_slot_order_holds_regardless_of_slot_order_in_the_fact`, `clips_deftemplate_applies_inside_defrule_conditions_and_conclusions`, `clips_deftemplate_works_end_to_end_through_forward_in` у `crates/my-lisp/tests/clips_import.rs`.

Крок 9 зроблено — знову спершу перевірка, не здогадка: правило `(defrule mass-rule (planet ?x) => (printout t "found planet " ?x crlf) (assert (has-mass ?x)))` — вкрай звичайний CLIPS-патерн (debug-вивід поряд із твердженням) — до цього кроку імпортувалось як **взагалі відсутність clause**, мовчки відкидаючи цілком представний `assert` лише через сусідній нешкідливий `printout`. `clips-drop-printouts` тепер фільтрує `printout`-форми перед перевіркою "чи всі решта — `assert`", тож `printout` більше не дискваліфікує правило. `retract`/`modify` і далі дискваліфікують — вони посилаються на факт за CLIPS fact-address, поняття, якого немає в моделі множини фактів цього проєкту (my-lisp порівнює факти структурно через `equal?`, не за ідентичністю), тож чесно імпортуються як відсутність clause, а не здогадка про непредставну семантику. Тести: `clips_defrule_with_printout_alongside_assert_still_imports_the_assert`, `clips_defrule_with_retract_still_imports_as_no_clauses` у `crates/my-lisp/tests/clips_import.rs`.

Крок 13 зроблено — перевіривши, чи Крок 8 (`deftemplate`) правильно взаємодіє з Кроками 10-11 (`or`/`and` з `lib/forward.my`): `clips-convert-template` рекурсувала лише крізь `not`, тож факт-шаблон, вкладений в `or`/`and` (`(or (cat (name ?x)) (dog (name ?x)))`), назавжди лишався в іменованій формі й ніколи не унікувався з позиційним фактом — мовчки нічого не виводило. Виправлено додаванням `or`/`and`-рекурсії в `clips-convert-template`, тим самим уроком, який Кроки 10-12 `lib/forward.my` вже кілька разів підтвердили: кожен логічний комбінатор потребує явної обробки, не лише перший знайдений. Тест: `clips_deftemplate_converts_facts_nested_inside_or` у `crates/my-lisp/tests/clips_import.rs`.

Крок 15 зроблено — за прямим проханням користувача: "імпортер нам потрібний, щоб перевірити, чи наш символьний AI може спілкуватися з іншими". Замість вигаданого прикладу — реальний зовнішній `.clp`-файл, завантажений з офіційних прикладів CLIPS (класична задача GERALD+DONALD=ROBERT, `tests/fixtures/wordgame-external.clp`, не написаний нами). Спроба імпорту "як є" одразу виявила справжній баг: реальний CLIPS `assert` приймає **кілька** фактів за один виклик (`(assert (number 0) (number 1) ... (letter T))` — 20 фактів в одному `assert`), а наш `clips-assert-conclusions` читав лише перший аргумент кожної форми `assert`, мовчки відкидаючи решту 19. Виправлено: `(cdr (car forms))` замість `(second (car forms))`, `append`-нуто через усі форми `assert`. Після фіксу весь файл (усі 20 фактів + правило `generate-combinations`) імпортується коректно; правило `find-solution` (лише `printout`, без `assert`, плюс `test`/`&~`-конструкції, яких ми не підтримуємо) свідомо не дає жодного clause — не баг, у нього справді нічого стверджувати. Тести: `clips_defrule_with_a_multi_fact_assert_produces_one_clause_per_fact`, `clips_import_file_imports_a_real_external_clp_file_from_the_official_clips_examples` у `crates/my-lisp/tests/clips_import.rs`.

Крок 16 зроблено — другий реальний зовнішній `.clp`-файл, за прямим проханням "спробуй ще один". Завантажено власний приклад CLIPS "Automotive Expert System" (`tests/fixtures/auto-external.clp`, 21 правило). Спроба "як є" одразу показала: **кожне** з 21 правил мало порожній докстрінг `""` одразу після імені (звичайний CLIPS-стиль), кілька — ще й `(declare (salience N))`; жодне з двох ми не обрізали, тож обидва потрапляли в список умов як сторонні терми, які ніколи ні з чим не зіставляться — увесь файл був мертвий, не одне правило. Додано новий примітив `string?` (за аналогією з `symbol?`, `crates/my-lisp/src/eval/special_forms.rs`), щоб відрізнити докстрінг від звичайної умови; `clips-strip-rule-preamble` тепер обрізає докстрінг і/або `declare` перед тим, як `clips-split-at-arrow` бачить умови. Salience (пріоритет спрацювання CLIPS) свідомо не має відповідника — `run-multi`/`run-jtms-multi` матеріалізують усе до fixpoint незалежно від порядку, тож відкидання `declare` втрачає інформацію про *коли*, ніколи не про *чи*. Тести: `clips_defrule_strips_a_docstring_before_its_conditions`, `clips_defrule_strips_a_declare_salience_before_its_conditions`, `clips_defrule_strips_both_docstring_and_declare_together`, `clips_import_file_imports_a_second_real_external_clp_file_correctly` у `crates/my-lisp/tests/clips_import.rs`; `string_predicate_distinguishes_strings_from_other_atoms` у `crates/my-lisp/tests/mccarthy.rs`.

Крок 17 зроблено — третій реальний зовнішній `.clp`-файл; астрономічного прикладу для CLIPS не знайшлося ніде (перевірено двома окремими пошуками), тож обрано інший готовий приклад з тієї ж офіційної бібліотеки. Завантажено власний приклад CLIPS "Wine Expert Sample Problem" (`tests/fixtures/wine-external.clp`, 392 рядки). Спроба "як є" виявила одразу дві реальні помилки (кожна підтверджена через тимчасовий debug-тест перед фіксом, за встановленою методологією). Перша: цей файл — перший, що використовує `defmodule`-простори імен для `deftemplate` (`(deftemplate QUESTIONS::question ...)`), тоді як відповідні `deffacts` асертують факти голим іменем (`(question ...)`) — CLIPS сам резолвить це відносно поточного модуля, а наш імпортер модулів не знає взагалі, тож пошук шаблону через `clips-assoc` (звичайний `eq`) ніколи не збігався, і конвертація іменованих слотів у позиційну форму мовчки не спрацьовувала для жодного факту цього шаблону. Виправлено: нові `clips-strip-module-prefix`/`clips-string-after-last-double-colon`/`clips-string-starts-with-double-colon?`/`clips-string-empty?` (чиста my-lisp на основі вже наявних `string-first`/`string-rest`/`symbol->string`/`string->symbol`, без нових Rust-примітивів) обрізають `MODULE::`-префікс і при реєстрації імені шаблону, і при пошуку за головою факту — обидва боки порівнюються вже без префіксу. По дорозі впіймано власний баг у першій версії цього хелпера: `(atom after)` як перевірка "не знайдено" хибно спрацьовувала і на успішний результат-рядок (рядок — теж atom у цій мові), бо `atom` означає "не пара", а не "порожній список"; виправлено на явне `(eq after '())`. Друга помилка: CLIPS-умова може називати мультислот зовсім без значення (`(precursors)` — "збігається незалежно від вмісту, навіть порожнього"), а `clips-slot-value-of` беззастережно викликав `second` на такій формі, падаючи з `car expects a non-empty list` на списку без `cdr`; виправлено тим самим guard-паттерном "пропустити, не впасти", що й скрізь у файлі — валюється в `'()` (без обмеження) замість падіння. Тести: `clips_deftemplate_name_is_matched_regardless_of_a_defmodule_qualifier`, `clips_condition_slot_with_no_value_does_not_crash_the_importer`, `clips_import_file_imports_a_third_real_external_clp_file_with_module_qualified_templates` у `crates/my-lisp/tests/clips_import.rs`.

Крок 18 зроблено — четвертий реальний зовнішній `.clp`-файл, за прямим проханням "спробуй ще один зовнішній .clp файл". Завантажено власний приклад CLIPS "Animal Identification Expert System" (`tests/fixtures/animal-external.clp`, 634 рядки, 37 верхньорівневих форм, одна `deffacts`-база знань зі 128 фактів). Спроба "як є" не видала помилку — вона **впала весь процес** з `STATUS_STACK_OVERFLOW`, найсерйозніший клас бага з усіх дотепер (перевірено бісекцією через тимчасовий debug-тест: кожна форма окремо імпортувалась без проблем, крах ставався лише коли весь список форм проходив через `clips-import-forms` за один виклик). Корінь: `clips-facts->clauses` (по фактах усередині `deffacts`) і `clips-import-forms` (по верхньорівневих формах) будували результат патерном `(cons ... (recurse ...))`/`(append ... (recurse ...))` — жоден із двох не є хвостовим викликом, тож Rust-стек ріс на один фрейм на елемент, і для 128-фактового блоку (додатково вкладеного в 37-рівневу рекурсію по формах) цього вистачило для реального переповнення. Це саме та проблема, яку `lib/core.my`'s власні `map`/`filter`/`append`/`length` вже намагаються уникнути через `-onto`-акумулятор + `reverse` (задокументовано й регресійно протестовано в `crates/my-lisp/tests/stack_safety.rs` на 100-тисячному списку) — `lib/clips-import.my` просто не наслідував цей паттерн для своїх власних рекурсій. Виправлено: `clips-facts->clauses-onto` і `clips-import-forms-onto` (плюс маленький хелпер `clips-cons-each-onto` для сплющення списку clause однієї форми на акумулятор) переписані в тому самому хвостово-рекурсивному стилі; порядок результату не змінився (перевірено — усі попередні 27 тестів пройшли без жодної зміни очікуваних рядків). Тести: `clips_import_stays_stack_safe_on_a_deffacts_block_with_many_facts` (синтетичний 5000-фактовий `deffacts`, не залежить від зовнішнього фікстура — за зразком власного стилю `stack_safety.rs`), `clips_import_file_imports_a_fourth_real_external_clp_file_with_a_large_deffacts_block` у `crates/my-lisp/tests/clips_import.rs`.

П'ятий реальний зовнішній `.clp`-файл (без нового номера кроку — коду не мінялося, лише покриття), за прямим проханням "спробуй ще один зовнішній .clp файл": класичний OPS5-бенчмарк "Manners Benchmark" (`tests/fixtures/manners-external.clp`, 158 рядків, 8 правил, жодного `deffacts` — цей бенчмарк у реальному CLIPS завантажує дані гостей окремо). На відміну від попередніх чотирьох файлів, цей **не** знайшов жодного нового бага — чиста перевірка: `defglobal` коректно ігнорується (не `deffacts`/`defrule`, не дає clause); 6 із 8 правил мають `modify`/`retract`/`(halt)` на правій стороні й коректно імпортуються як відсутність clause за вже задокументованою політикою (Крок 9); одна умова навіть використовує CLIPS-синтаксис зв'язкового обмеження `~?s1` (задокументовано як непідтримуваний — читається як звичайна дивно названа змінна), але це в правилі, яке й так дискваліфіковане через `modify`, тож не мало значення. Лишається рівно одне правило (`make_path`, чиста `assert`-консеквента) — результат зафіксовано регресійним тестом як доказ, що наявна логіка вже правильно тримає купу різних кейсів одночасно на іншому за формою файлі, без крашу. Тест: `clips_import_file_imports_a_fifth_real_external_clp_file_with_mostly_disqualified_rules` у `crates/my-lisp/tests/clips_import.rs`.

Шостий реальний зовнішній `.clp`-файл, знову за прямим проханням "спробуй ще один": "Farmer's Dilemma" (`tests/fixtures/dilemma1-external.clp`, класична задача про фермера/лисицю/козу/капусту, 197 рядків, три `defmodule` — MAIN/CONSTRAINTS/SOLUTION). Теж без нового бага — ще одне чисте підтвердження. Усі 4 правила "генерації шляху" використовують `duplicate` (дію CLIPS, яку імпортер бачить уперше — потрапляє в ту саму категорію "не `assert`", що й `halt`); решта правил мають `retract`/`modify` на правій стороні — усі коректно дають відсутність clause. Одна умова містить `?sd2&:(< ?sd1 ?sd2)` — тест-обмеження, приліплене до символу без пробілу; наш reader розбиває це на зайвий сторонній елемент списку замість крашу, і оскільки те саме правило (`circular-path`) і так дискваліфіковане через власний `retract`, це ніде не проявляється. Вижили лише два `deffacts`-блоки (`MAIN::status`/`MAIN::opposites`) — знову перевірили Крок 17 (`defmodule`-кваліфіковане ім'я `status` коректно зіставляється з голими фактами). Тест: `clips_import_file_imports_a_sixth_real_external_clp_file_with_duplicate_and_embedded_constraints` у `crates/my-lisp/tests/clips_import.rs`.

Пробували й восьмий кандидат — `dilemma2.clp` (та сама задача про фермера, але переписана через CLIPS Object-Oriented Language: `defclass`/`defmessage-handler`, без жодного `deffacts`). Результат — порожньо, без крашу: `defclass`/`defmessage-handler` коректно ігноруються як невідомі top-level форми, єдиний `defrule` (нуль умов, RHS — виклик функції, не `assert`) коректно дискваліфікується. Нічого структурно нового понад уже покрите "невідома форма → пропустити", тому фікстуру й тест не додавали — цей файл лише підтвердив межу підходу: об'єктно-орієнтований CLIPS (COOL) — інша парадигма, поза моделлю "множина фактів + правила".

Сьомий (за нумерацією тестів) реальний зовнішній `.clp`-файл: класична головоломка "Хто власник зебри?" (`tests/fixtures/zebra-external.clp`, 209 рядків). Без нового бага, але перша перевірка імпортера на реальному масштабі, а не лише в ізоляції. `find-solution` — 25 умов, усі проти одного шаблону `(deftemplate avh (field a) (field v) (field h))` (зауважити: `field`, не `slot` — не має значення для імпортера, бо той читає лише саму назву слота, а не ключове слово перед нею); більшість умов обгорнуті в зв'язкові обмеження CLIPS (`~?n1`, `&=(+ ?c2 1)`, `&:(or ...)`) — уже відомий, задокументований ліміт (читається як дивне ім'я змінної або зайвий сторонній елемент списку), підтверджено безпечним і на масштабі 25 умов одного шаблону, не лише поодиноко. Друге: `startup` — перше **правило, що вижило**, з нульовою кількістю умов ліворуч узагалі (одразу `=>`); попередні нуль-умовні правила (`wine.clp`'s `start`, `dilemma2.clp`'s `start-it`) завжди дискваліфікувались з іншої причини раніше, ніж цей шлях мав значення. `print-solution`/`generate-combinations` коректно дискваліфіковані через `retract`. Тест: `clips_import_file_imports_a_seventh_real_external_clp_file_at_scale` у `crates/my-lisp/tests/clips_import.rs`.

Восьмий реальний зовнішній `.clp`-файл: "Monkees and Bananas" (`tests/fixtures/mab-external.clp`, 368 рядків, 33 `defrule`, кожне з порожнім докстрінгом `""`). Без нового бага, але перша поява **голого** `~var`/`~symbol` зв'язкового обмеження як цілого значення слота, а не лише хвостом `&`-ланцюжка на кшталт `?x&~?y` (як у попередніх файлах) — напр. `(monkey (holding ~?chest))`, `(thing (on-top-of ~floor) ...)`. Той самий уже відомий ліміт (читається як дивно сформований символ, не обчислюється) підтверджено безпечним і в цій новій формі. Також перший файл із 33 правилами одночасно, де `not`-умови, fact-address-прив'язки (`?monkey <-`) і `modify` на правій стороні взаємодіють масово: кожне правило з `modify` коректно дискваліфікується, кожне чисто-`assert`-правило (усі з порожнім докстрінгом) коректно виживає — 17 правило-похідних + 13 фактів з `startup` = 30 клауз. Тест: `clips_import_file_imports_an_eighth_real_external_clp_file_with_bare_tilde_constraints` у `crates/my-lisp/tests/clips_import.rs`.

Спробували й `dilemma3.clp` — та сама фермерська задача, знову через COOL (`(object (is-a status) ...)`-зіставлення замість `deftemplate`-фактів, `duplicate-instance` замість `duplicate`). Результат — порожньо, без крашу, нічого нового понад уже підтверджену межу підходу (об'єктно-орієнтований CLIPS — поза моделлю); без окремого тесту.

Крок 19 зроблено — дев'ятий реальний зовнішній `.clp`-файл: власний Sudoku-solver CLIPS (`tests/fixtures/sudoku-external.clp`, 357 рядків, 12 `defrule`, без `deffacts` — дані пазла завантажуються окремо). Спроба "як є" виявила справжній баг, підтверджений через тимчасовий debug-тест перед фіксом (за встановленою методологією): одне правило містило `(exists (unsolved))` — квантифікований умовний елемент CLIPS ("істинно, якщо існує принаймні один факт `unsolved`"), якому немає відповідника в диспетчері умов `lib/forward.my` (`match-one-condition` розпізнає лише `not`/`or`/`and`/`test`). Без охорони ця умова мовчки проходила як звичайний терм і потрапляла в `match-condition-against-facts`, намагаючись уніфікувати факт із головою буквально `exists` — якого ніколи не існує — тож усе правило мовчки ніколи б не спрацювало. Той самий клас "мовчки-мертвого-правила" бага, що й `not`/`or`/`and`/`test` у Кроках 5/7/10-12. Виправлено тим самим шляхом, що й початковий (до-Кроку-7) guard для `not`: нові `clips-condition-is-exists-or-forall?`/`clips-any-exists-condition?` виявляють `exists`/`forall` (обидва боки квантифікатора, про всяк випадок) на верхньому рівні умов правила ще до конвертації, і все правило пропускається (порожній список clause) замість імпорту як мовчки зламаного. Реальна підтримка `exists`/`forall` у `lib/forward.my` — окремий, більший крок (зміна рушія, не імпортера), як і колись з `not` до Кроку 7. Тести: `clips_defrule_with_an_exists_condition_is_skipped_not_silently_broken`, `clips_defrule_without_an_exists_condition_still_imports_normally`, `clips_import_file_imports_a_ninth_real_external_clp_file_and_finds_the_exists_bug` у `crates/my-lisp/tests/clips_import.rs`.

Крок 4 зроблено — той самий capability boundary, який свідомо відклали в Кроці 3, тепер узгоджений і зроблений явно: читання **справжнього** `.clp`-файлу з диска. `load` уже читає файл, але виконує кожну знайдену форму — саме неправильно для CLIPS-коду, чиї `defrule`/`=>` не є осмисленою my-lisp-командою для *виконання*, лише для читання як дані. Два нових Rust-примітиви: `read-file` (сирий текст файлу, без парсингу/виконання) і `read-all` (багатоформний відповідник `read` — парсить рядок у кожну форму верхнього рівня як дані; `read` падає, якщо рядок не рівно одна форма). `clips-import-file` у `lib/clips-import.my` — просто `(clips-import (read-all (read-file path)))`. Створено справжню CLIPS-фікстуру [`tests/fixtures/astronomy.clp`](../tests/fixtures/astronomy.clp) (не вигаданий приклад у тесті, а реальний файл на диску) для наскрізної перевірки. Тести: `read_all_parses_every_top_level_form_as_data`, `read_all_rejects_a_non_string` у `crates/my-lisp/tests/mccarthy.rs`; `clips_import_file_reads_and_imports_a_real_clp_file` у `crates/my-lisp/tests/clips_import.rs`.

Крок 5 зроблено — не за прямим запитом користувача обрати цей крок, а за консультацією з `private/mccarthy-principles.md` (п.4/п.7: чесність про межі, не тиха неправильна поведінка) і **перевіркою перед фіксом**, а не здогадкою: написав тестовий сценарій `(defrule bird-rule (animal ?x) (not (penguin ?x)) => (assert (bird ?x)))` і побачив, що `tweety` (тварина, не пінгвін) мовчки не стає `bird` — `match-conditions` у `lib/forward.my` унікує кожну умову буквально проти фактів, а жоден факт не має головою `not`, тож конвертована `(not (penguin (var x)))` завжди провалюється. Backward-chaining (`prove-goals`) коректно обробляє `not`, forward-chaining — ще ні; розширення `lib/forward.my` до negation-as-failure — окремий, більший крок (зміна самого рушія, не імпортера), тут не мається на увазі. Виправлено найчеснішим способом: `clips-defrule->clauses` тепер виявляє будь-яку умову `(not ...)` через `clips-any-not-condition?` і пропускає все правило (порожній список clause) — видимий, безпечний результат замість тихо зламаної бази знань. Тести: `clips_defrule_with_a_not_condition_is_skipped_not_silently_broken`, `clips_defrule_without_a_not_condition_still_imports_normally` у `crates/my-lisp/tests/clips_import.rs`.

16. 🔶 **Ідентичність: `my-lisp` = `knowledge-lisp`/`MyLisp`, офіційно об'єднано.** `private/PROFILE.md` описував `knowledge-lisp/MyLisp` як окремий майбутній проєкт — "власна Lisp-подібна мова між Common Lisp і Scheme... атоми матриці знань... незалежна Rust-реалізація, окремо від IDE". До цього моменту жодна сесія не звірила це бачення з тим, чим `my-lisp` фактично став. Звірка (2026-08-09, за консультацією з `private/mccarthy-principles.md`, п.2 "не плодити несумісні діалекти" — коли дві підсистеми з подібною роллю ризикують мовчки розійтись, питати явно, не залишати на потім): `my-lisp` уже реалізує майже все, що `PROFILE.md` приписував окремому `knowledge-lisp` — "атом як вхід у поняття" (`describe` у `lib/knowledge.my`), модульні пакети знань за доменом (`defmodule`, `knowledge/astronomy.my`/`physics.my`, точно та структура, що й `knowledge/amateur-radio`/`electronics`/... з `private/PROJECT_MEMORY.md`), правила, якими знання взаємодіють (`lib/reason.my`, `lib/forward.my`, JTMS), і природна мова в обидва боки (`lib/understand.my`/`lib/narrate.my`) — усе це прямий опис "Advice Taker"-лінії з `private/lisp-to-knowledge.md`. Рішення: `my-lisp` і `knowledge-lisp`/`MyLisp` — **один проєкт**, не два; `PROFILE.md`'s окремий запис про `knowledge-lisp` вважати застарілим баченням, реалізованим тут. Одна реальна структурна різниця залишається невиправленою, і за принципом 7 ("робити чисто або писати статтю спершу" — спрощення мусить бути явно задокументоване з наступним кроком, не тихо забуте): **append-only fact journal**. `PROFILE.md` мислив дані знань як журнал, що тільки росте й ніколи не переписується; `lib/knowledge.my` зараз — чисто функціональні `def`-переприв'язки (immutable rebinding через макроси, не append-only лог). Наступний крок, коли до нього дійде черга: чи `*knowledge-base*`/модулі повинні стати списком-журналом подій (`tell`/`retract` як append, а не rebind), а не просто фіксованим списком clause — окреме, більше рішення про модель даних, не зроблено тут.

## Нотатки

- Гілка `my-lisp` у `my-idea`-репозиторії (origin) зараз ще існує (і локально, і на GitHub) — користувач раніше питав, чи прибрати її, раз є окремий репо. Рішення не ухвалене — уточнити на початку нової сесії.
- `private/` папка (PROJECT_MEMORY.md, PROFILE.md) з `my-idea` **не повинна** потрапити в новий репо — вона й так у `.gitignore`, але перевірити після filter-repo, що жоден слід не потрапив у git-історію.
