[package]
name = "my-lisp-host"
version = "0.31.0"
edition = "2021"
license = "MIT"
description = "Host capability layer for my-lisp: filesystem, processes, TCP"

[dependencies]
my-lisp = { path = "../my-lisp" }
