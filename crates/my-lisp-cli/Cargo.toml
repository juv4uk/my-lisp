[package]
name = "my-lisp-cli"
version = "0.26.0"
edition = "2021"

[[bin]]
name = "my-lisp"
path = "src/main.rs"

[dependencies]
my-lisp = { path = "../my-lisp" }
my-lisp-lsp = { path = "../my-lisp-lsp" }
rustyline = { version = "14.0.0", features = ["with-file-history"] }
