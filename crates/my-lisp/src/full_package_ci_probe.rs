// Temporary CI-only probe: this unreferenced module exists solely to classify
// the verification PR as a core Rust change so the full `cargo test -p my-lisp`
// lane runs against #282 repair head be872a0c. Synchronize after retarget; never merge.
