[package]
name = "my-lisp-embed"
version = "0.1.0"
edition = "2021"
license-file = "../../LICENSE"
description = "Canonical stateful my-lisp C ABI embedding boundary"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
my-lisp = { path = "../my-lisp" }
