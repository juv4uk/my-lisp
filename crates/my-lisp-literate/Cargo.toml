[package]
name = "my-lisp-literate"
version = "0.39.0"
edition = "2021"
license-file.workspace = true
description = "Literate markdown extraction wrapper for the my-lisp engine"

[dependencies]
my-lisp = { path = "../my-lisp" }
pulldown-cmark = "0.12"
