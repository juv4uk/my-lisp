# my-lisp test results · Результати тестів my-lisp · my-lisp-Testergebnisse

## English

This repository has one test layer: the four Rust crates under `crates/`, run with `cargo test --workspace`. There is no separate JS/web test suite here — the standalone web REPL (`public/my-lisp-cli-web.html`) is built from the same WASM crate covered below, and the previous Node/Playwright suite stayed behind in the `my-idea` IDE repo this crate set was extracted from (see [`docs/versioning.md`](versioning.md)). This table is the current source of truth and should be refreshed whenever a suite gains or loses tests.

| Crate | Suite | Tests | Covers | Result (last run) |
|---|---|---:|---|---|
| `my-lisp` | unit tests (`src/parser.rs`, `src/environment.rs`, `src/eval/mod.rs`, `src/error.rs`, `src/bignum.rs`) | 44 | reader/parser edge cases, lexical-scope isolation, single-pass evaluation, macro expansion, char-based line/column and caret rendering for structured errors, the hand-rolled arbitrary-precision `BigInt` (add/sub/mul/div/gcd/ordering/decimal parsing and formatting) backing `Rational`, the reader's dotted-pair syntax (`ExprKind::Pair`) — `(a b . c)` parses as nested pairs, a lone `.` between two sub-expressions inside parentheses, with clean parse errors for a dangling `.` on either side or extra tail expressions, while a bare top-level `.` (no list context) still reads as an ordinary symbol — and the numeric-exactness literal rule (`Exactness`): an integer literal parses exact at arbitrary precision, a decimal or exponential-notation literal parses inexact, regardless of trailing zeros (`3`/`3.0`/`3.00`/`3e0`) | ok |
| `my-lisp` | `tests/mccarthy.rs` | 85 | the seven McCarthy primitives, exact/inexact arithmetic, `<`/`>`/`=` kernel comparison chaining plus `<=`/`>=` moved into core.my, `print`'s session-wide output transcript (including through closures), `read`/`eval` closing the read-eval loop by hand, lambda semantics, structured errors, `lib/core.my` list utilities (`length`, `reverse`, `append`, `map`, `filter`, `reduce`), `let`/`let*`, `equal?`, exact arithmetic past `i64` range (factorial of 30, computed exactly), the minimal symbol/string introspection added for `lib/clips-import.my` — `symbol?`/`string?`, `symbol->string`/`string->symbol`, `string-first`/`string-rest` — `read-all`, the multi-form counterpart to `read`, the dotted-pair reader fix, a sync check confirming `my-lisp-constitution.my` hasn't drifted from `tests/fixtures/conformance.my`, the process guard for the symbolic-reasoning layer, and variadic lambda/`defmacro` parameters (dotted `(a b . rest)` lists and bare-symbol `args`) — the change that let `list` itself move from a Rust special form to `lib/core.my`, and opt-in resource limits (`Environment::with_cons_limit`/`with_numeric_bit_limit`) making `OutOfMemory`/`NumericOverflow` real, tested `ErrorKind` categories, and `write-to-string` serialization, and `write-file` (round-tripping with `read-file`, overwrite-not-append semantics, structured errors on a non-string path/content or wrong arity), and `string-append`/`string<?` (concatenation, lexicographic ordering, structured errors on non-string arguments or wrong arity), and `process-run` (disabled by default, structured errors when the session never opted into an allowlist, running an explicitly allowed program, rejecting a program not on the allowlist, and rejecting non-string program/argument values) | ok |
| `my-lisp` | `tests/stack_safety.rs` | 6 | tail recursion and deep list clone/drop use constant Rust stack, `lib/core.my`'s `append`/`filter`/`map`/`length` stay stack-safe on a 100,000-element list | ok |
| `my-lisp-cli` | `tests/cli.rs` | 12 | the compiled binary end-to-end: `--version`/`--help`, file execution, parse/eval error exit codes, missing-file handling, `lib/core.my` preloading, REPL history persisting to `~/.my-lisp-history` across separate sessions, `(read)` reading one line from real piped stdin in file mode, and `*argv*` (everything after the filename, as a my-lisp list of strings, for `scripts/release.my`'s version argument — empty, not an error, when nothing follows the filename) | ok |
| `my-lisp` | `tests/meta_eval.rs` | 14 | `lib/meta-eval.my`, the metacircular evaluator: self-evaluation, `quote`, arithmetic/list-primitive dispatch, `cond`, lambda application (single- and multi-expression bodies), closures capturing free variables from a passed-in env, higher-order functions, `def`/`defmacro` via `my-eval-program`, loading a verbatim slice of `lib/core.my` and running it through `my-eval`, the documented self-recursive-top-level-`def` limit | ok |
| `my-lisp` | `tests/unify.rs` | 12 | `lib/unify.my`, the unification primitive: atom matching, variable binding/resolution, structural (compound-term) unification, mismatch failure, transitive chained-variable resolution, same-variable unification creating no binding, full-query `apply-subst`, occurs-check, `thread-conjunction` — the conjunction-walking kernel `lib/reason.my` and `lib/forward.my` both build their conjunctive matching on | ok |
| `my-lisp` | `tests/reason.rs` | 13 | `lib/reason.my`, the backward-chaining engine: simple facts, recursive rules, standardizing apart, negation as failure, proof trees and `explain-proof` trace output, `reason-explain`'s "provable" vs "cannot prove" distinction, `count-usage` walking a proof tree into a `(rule-head . times-used)` tally, `provenance` turning a proof node into a `(statement goal (source fact\|rule) (rule ...) (derived-from ...))` record | ok |
| `my-lisp` | `tests/knowledge.rs` | 42 | `lib/knowledge.my`, modular knowledge packages built on an append-only `*knowledge-journal*` (replacing the old `*knowledge-base*` snapshot): guarded `advise`, atomic `advise-all`, and versioned data-only `my-lisp-knowledge` package import/export, including validation, rollback on any malformed clause, internal and derived contradiction detection across proposed and existing knowledge; `defmodule`/`tell-knowledge` push `tell` events, `module-clauses-now` projects a module's current clauses by folding its events, `retract-knowledge` (a capability the snapshot model never had at all) pushes `retract` events, `module-known?` distinguishes "never told anything" from "told, then retracted down to nothing" so `reason-in`/`forward-in`/`describe` keep returning `Module-not-found` only for the former, isolated per-module queries via `reason-in`, conflict detection in `tell-knowledge` (deliberately not applied to `retract-knowledge` — removing a clause can't itself contradict anything), `describe` collecting every known fact about a symbol (the "atom as concept entry point" idea), `record-usage!`/`usage-of` accumulating rule-usage counts across separate top-level queries, `forward-in` running `lib/forward.my`'s `run-multi` over a module's projected clause list to a fixpoint, calling `defmodule`/`tell-knowledge` twice for the same module name accumulating both calls' clauses instead of the old snapshot silently discarding the first, plus `knowledge/family.my` (a three-generation family tree with a recursive `ancestor` rule, alongside `knowledge/physics.my`/`astronomy.my`'s single-hop domains) proving `reason`/`unify`'s recursive-logic capability actually fires a rule against its own prior output, not just once per rule | ok |
| `my-lisp` | `tests/clips_import.rs` | 36 | `lib/clips-import.my`, Steps 1-5, 8-9, 13, 15-19 (Step 5 superseded by `lib/forward.my` Step 7; Step 19's `exists`/`forall` skip-guard superseded by `lib/forward.my` Step 15) of an importer for old symbolic-AI systems (starting with CLIPS, NASA JSC 1985): converts real CLIPS `deffacts`/`defrule` forms into `defmodule`-ready clauses, `?x` variables, multi-fact `assert` calls, `deftemplate`'s named slots, `printout` no longer disqualifying a rule, stripping a `defrule`'s optional docstring/`(declare (salience N))` preamble before its conditions, `defmodule`-qualified deftemplate names, valueless multislot conditions, a real Rust stack overflow on large `deffacts` blocks (found via `tests/fixtures/animal-external.clp`), fixed with `-onto`-accumulator tail recursion, a fifth/sixth/eighth genuine external `.clp` file (`manners`/`dilemma1`/`mab`) confirming existing policy at increasing scale, a seventh (`zebra`) exercising the importer at real scale, and a ninth (`tests/fixtures/sudoku-external.clp`, CLIPS's own Sudoku solver) whose `(exists (unsolved))` condition now imports and converts correctly — `clips-convert-template` learned to recurse into `exists`/`forall` sub-conditions too, the same lesson Step 13 already taught for `not`/`or`/`and` | ok |
| `my-lisp` | `tests/understand.rs` | 5 | `lib/understand.my`, the controlled-natural-language bridge: fixed word-list shapes (`X is a Y`, `X V Y`, `all X have Y`) mapped to knowledge clauses, and the result fed straight into `reason` with no hand-editing step | ok |
| `my-lisp` | `tests/forward.rs` | 44 | `lib/forward.my`, Steps 1-7, 10-12, 14, and 15 of a CLIPS-style forward-chaining engine: `assert-fact!` growing the global working memory, `fire-rule` unifying a rule's pattern against one fact, `fire-rule-on-facts`/`fire-rule-on-working-memory` applying one rule across a whole fact list, `fire-rules-on-facts`/`fire-rules-on-working-memory` applying an entire rule set in one pass, `run` looping rules to a fixpoint, `assert-facts!` merging `run`'s result back into `*working-memory*`, `retract-fact`/`retract-fact!` removing one fact by `equal?`, a single-justification truth maintenance system, a full multi-justification JTMS, `fire-rule-multi`/`fire-rules-multi`/`run-multi` sharing `lib/reason.my`'s exact `(head cond1 cond2 ...)` rule shape, (Step 7) negation as failure, (Step 10) `or` conditions, (Step 11) `and` conditions, (Step 12) `test` conditions, (Step 14) a multi-condition JTMS twin (`run-jtms-multi!`), and (Step 15) `exists`/`forall` quantified conditional elements — found missing by importing a real external CLIPS file (`tests/fixtures/sudoku-external.clp`), the same silently-dead-rule bug class as `not`/`or`/`and`/`test`; `exists` succeeds on any match of its sub-conditions without binding new variables outward (built on `match-conditions`, discarding whichever sub-substitution satisfied it); `forall` — after a first attempt built on `not`/`and` composition failed a debug test, because `match-negated-condition` never recurses through `match-one-condition` for compound inner patterns — is written directly: every substitution the first condition produces must also satisfy the rest | ok |
| `my-lisp` | `tests/narrate.rs` | 7 | `lib/narrate.my`, the "structure -> text" half of the bridge (reverse of `lib/understand.my`): `narrate-fact` as the exact structural inverse of `understand-is`/`understand-relation` (round-trips through `understand`), `narrate-provenance` walking a `provenance` record into a `because`/`and`-joined word-list explanation, and a dedicated regression test pinning the documented limitation that a derived rule head's own `(var ...)` placeholders stay unresolved (`provenance` doesn't carry the query's final substitution) | ok |
| `my-lisp` | `tests/persistent_map.rs` | 8 | `lib/persistent-map.my`, the AVL-balanced persistent map: empty-map lookups, insert-then-get, the "maybe" shape distinguishing a stored `()` value from absence, insert replacing rather than duplicating an existing key, persistence (the original tree untouched after building a new one from it), in-order `map->list` returning sorted keys, and — the actual point of choosing a balanced tree — seven keys inserted in already-sorted order (the textbook worst case for an unbalanced tree) staying at height 3 instead of degenerating to a height-7 linked list | ok |
| `my-lisp` | `tests/tcp.rs` | 11 | the raw TCP socket primitives (`tcp-connect`/`tcp-listen`/`tcp-accept`/`tcp-read`/`tcp-write`/`tcp-close`): a real client/server round trip across two OS threads with separate `Session`s (server accepts, reads, echoes, closes; client connects, writes, reads the echo, closes), `tcp-read` returning `""` on a closed connection instead of erroring, connecting to a port nothing listens on failing named (`InvalidForm`) rather than hanging, `tcp-write` returning its content argument unchanged, and structured `Type` errors for a non-string host, an out-of-range port, and a non-connection argument | ok |
| `my-lisp-literate` | `tests/literate_offsets.rs` | 4 | literate-Markdown source-offset mapping | ok |
| `my-lisp-wasm` | unit test (`src/lib.rs`) | 1 | the WASM adapter produces the same exact/single-pass evaluation struct as the native core | ok |
| `my-lisp` | `tests/advice.rs` | 2 | end-to-end `understand → advise → reason-in → narrate-answer`, including rejection without journal mutation | ok |
| `my-lisp` | `tests/world.rs` | 54 | `lib/world.my`: immutable history and identity plus all principal legacy writers over World transitions, including atomic rollback and exactly-once evaluation of guarded macro arguments | ok |
| `my-lisp` | `tests/content_store.rs` | 5 | `lib/content-store.my`: immutable address→content storage, retrieval, deduplication, and preservation of distinct World histories | ok |
| **Total** | | **405** | | **405 passed, 0 failed, 0 ignored** |

The implementation-independent conformance fixture at [`tests/fixtures/conformance.my`](../tests/fixtures/conformance.my) — its own format and rules are in [`tests/fixtures/README.md`](../tests/fixtures/README.md) — is included directly into `crates/my-lisp/tests/mccarthy.rs` via `include_str!` and is exercised as part of that suite, not counted separately. The suite also pins `symbol?` as a `lib/core.my` function rather than a Rust built-in.

```bash
cargo test --workspace
```

Last recorded run: 2026-08-11, Linux x86_64, Rust/Cargo 1.93.0 from Guix — all passing, 0 failed, 0 ignored.

**Reader caveat resolved**: the reader previously had no dotted-pair literal syntax — `'(p . 0)` parsed `.` as an ordinary symbol, producing a 3-element proper list rather than a real dotted pair, even though the printer already rendered true dotted pairs exactly that way. Fixed 2026-08-09 (`ExprKind::Pair`, see the unit-test row above) — a quoted `'(p . 0)` and an actual `(cons 'p 0)` are now `equal?`.

## Українська

Цей репозиторій має один шар тестів: чотири Rust-крейти в `crates/`, що запускаються через `cargo test --workspace`. Окремого JS/web тест-набору тут немає — автономний web-REPL (`public/my-lisp-cli-web.html`) збирається з того самого WASM-крейта, покритого нижче, а попередній Node/Playwright-набір лишився в репозиторії IDE `my-idea`, з якого виділено цей набір крейтів (див. [`docs/versioning.md`](versioning.md)). Ця таблиця є поточним джерелом правди й має оновлюватися щоразу, коли набір отримує або втрачає тести.

| Крейт | Набір | Тестів | Покриває | Результат (останній запуск) |
|---|---|---:|---|---|
| `my-lisp` | unit-тести (`src/parser.rs`, `src/environment.rs`, `src/eval/mod.rs`, `src/error.rs`, `src/bignum.rs`) | 44 | межові випадки reader/parser, ізоляцію лексичного скоупу, однопрохідне обчислення, розкриття макросів, char-based рядок/стовпець і рендер "^" для структурованих помилок, власноруч написаний `BigInt` довільної точності (add/sub/mul/div/gcd/порівняння/парсинг і форматування десяткового рядка), що лежить під `Rational`, dotted-pair-синтаксис reader'а (`ExprKind::Pair`) — `(a b . c)` парситься як вкладені пари, одиночна `.` між рівно двома під-виразами всередині дужок, з чистими помилками парсингу на висячу `.` з будь-якого боку чи зайві хвостові вирази, тоді як голий `.` поза списком і далі читається як звичайний символ — і правило exactness для числових літералів (`Exactness`): цілий літерал парситься exact із довільною точністю, десятковий чи літерал в експоненційній нотації — inexact, незалежно від кількості нулів наприкінці (`3`/`3.0`/`3.00`/`3e0`) | ok |
| `my-lisp` | `tests/mccarthy.rs` | 85 | сім примітивів Маккарті, точну/неточну арифметику, ядрові `<`/`>`/`=` і перенесені в core.my `<=`/`>=`, транскрипт виводу `print`, спільний на сесію (включно через замикання), `read`/`eval`, що замикають read-eval цикл вручну, семантику lambda, структуровані помилки, list-утиліти `lib/core.my` (`length`, `reverse`, `append`, `map`, `filter`, `reduce`), `let`/`let*`, `equal?`, точну арифметику за межею `i64` (факторіал 30, обчислений точно), мінімальну інтроспекцію символів/рядків для `lib/clips-import.my`, `read-all`, фікс dotted-pair reader'а, перевірку синхронізації `my-lisp-constitution.my` з `tests/fixtures/conformance.my`, процесну гарантію символьного шару, і варіативні параметри `lambda`/`defmacro` (dotted `(a b . rest)`-списки й голий символ `args`) — зміну, що дозволила самому `list` перейти зі спеціальної форми Rust у `lib/core.my`, і опційні межі ресурсу (`Environment::with_cons_limit`/`with_numeric_bit_limit`), що зробили `OutOfMemory`/`NumericOverflow` реальними, перевіреними категоріями `ErrorKind`, і серіалізацію `write-to-string`, і `write-file` (round-trip з `read-file`, семантика перезапису-не-дописування, структуровані помилки на нерядковому шляху/вмісті чи неправильній арності), і `string-append`/`string<?` (конкатенація, лексикографічне впорядкування, структуровані помилки на нерядкових аргументах чи неправильній арності), і `process-run` (типово вимкнений, структуровані помилки, коли сесія ніколи не вмикала allowlist, запуск явно дозволеної програми, відхилення програми поза allowlist, і відхилення нерядкових значень програми/аргументів) | ok |
| `my-lisp` | `tests/stack_safety.rs` | 6 | хвостову рекурсію та clone/drop глибоких списків зі сталим Rust-стеком, `append`/`filter`/`map`/`length` з `lib/core.my` лишаються stack-safe на списку зі 100 000 елементів | ok |
| `my-lisp-cli` | `tests/cli.rs` | 12 | скомпільований бінарник наскрізно: `--version`/`--help`, виконання файлу, коди виходу при помилках парсингу/обчислення, відсутній файл, попереднє завантаження `lib/core.my`, збереження історії REPL у `~/.my-lisp-history` між окремими сесіями, `(read)` читає один рядок зі справжнього переданого через pipe stdin у файловому режимі, і `*argv*` (усе після імені файлу, як my-lisp-список рядків, для аргументу версії `scripts/release.my` — порожній, не помилка, коли нічого не йде після імені файлу) | ok |
| `my-lisp` | `tests/meta_eval.rs` | 14 | `lib/meta-eval.my`, метациркулярний evaluator: self-evaluation, `quote`, диспетчеризацію арифметики/list-примітивів, `cond`, застосування lambda (одно- й багатовиразові тіла), замикання, що захоплюють вільні змінні з переданого env, функції вищого порядку, `def`/`defmacro` через `my-eval-program`, завантаження дослівного фрагмента `lib/core.my` й виконання його через `my-eval`, задокументовану межу рекурсивного верхньорівневого `def` | ok |
| `my-lisp` | `tests/unify.rs` | 12 | `lib/unify.my`, примітив unification: зіставлення атомів, зв'язування/розв'язування змінних, структурну (composite-term) унікацію, провал при невідповідності, транзитивне розв'язування ланцюжка змінних, унікацію змінної з собою без зв'язку, повний `apply-subst` запиту, occurs-check, `thread-conjunction` — ядро обходу кон'юнкції, на якому будують кон'юнктивне зіставлення й `lib/reason.my`, і `lib/forward.my` | ok |
| `my-lisp` | `tests/reason.rs` | 13 | `lib/reason.my`, рушій логічного висновування: прості факти, рекурсивні правила, standardizing apart, negation as failure, дерева доведень і трасування `explain-proof`, розрізнення "доведено"/"не можу довести" в `reason-explain`, `count-usage` — обхід дерева доведення в таблицю `(голова-правила . скільки-разів-використано)`, `provenance` — перетворення вузла дерева доведення на запис `(statement ціль (source fact\|rule) (rule ...) (derived-from ...))` | ok |
| `my-lisp` | `tests/knowledge.rs` | 42 | `lib/knowledge.my`, модульні пакети знань на основі append-only `*knowledge-journal*`: захищений `advise`, атомарний `advise-all` і версіонований data-only імпорт/експорт пакетів `my-lisp-knowledge`, включно з валідацією, відкатом при будь-якому хибному clause і виявленням внутрішніх та вивідних суперечностей між новим і чинним знанням; `defmodule`/`tell-knowledge` пишуть події `tell`, `module-clauses-now` — проекція поточних clause модуля через згортку його подій, `retract-knowledge` пише події `retract`, `module-known?` розрізняє "нікому нічого не казали" від "казали, потім прибрали все" — тож `reason-in`/`forward-in`/`describe` дають `Module-not-found` лише для першого випадку, ізольовані запити по модулю через `reason-in`, виявлення конфліктів у `tell-knowledge`, `describe` — збір усіх відомих фактів про символ, `record-usage!`/`usage-of` — накопичення лічильників використання правил, `forward-in` — запуск `run-multi` до fixpoint, плюс `knowledge/family.my` із рекурсивним правилом `ancestor` | ok |
| `my-lisp` | `tests/clips_import.rs` | 36 | `lib/clips-import.my`, Кроки 1-5, 8-9, 13, 15-19 (Крок 5 замінено Кроком 7 `lib/forward.my`; guard `exists`/`forall` з Кроку 19 замінено Кроком 15 `lib/forward.my`) імпортера зі старих символьних AI-систем (починаючи з CLIPS, NASA JSC 1985): конвертує CLIPS `deffacts`/`defrule` у clause для `defmodule`, синтаксис `?x`, багатофактові виклики `assert`, іменовані слоти `deftemplate`, `printout`, що більше не дискваліфікує правило, обрізання необов'язкового докстрінгу й `(declare (salience N))` перед умовами `defrule`, `defmodule`-кваліфіковані імена шаблонів, мультислот-умови без значення, справжній stack overflow у Rust на великих блоках `deffacts` (знайдено на `tests/fixtures/animal-external.clp`), виправлено хвостово-рекурсивним акумулятором `-onto`, п'ятий/шостий/восьмий справжні зовнішні `.clp`-файли (`manners`/`dilemma1`/`mab`), що підтвердили наявну політику на зростаючому масштабі, сьомий (`zebra`), що перевірив імпортер на реальному масштабі, і дев'ятий (`tests/fixtures/sudoku-external.clp`, власний Sudoku-solver CLIPS), чия умова `(exists (unsolved))` тепер імпортується й конвертується коректно — `clips-convert-template` навчилась рекурсувати й у під-умови `exists`/`forall`, той самий урок, що Крок 13 уже дав для `not`/`or`/`and` | ok |
| `my-lisp` | `tests/understand.rs` | 5 | `lib/understand.my`, місток контрольованої природної мови: фіксовані форми списку слів (`X is a Y`, `X V Y`, `all X have Y`), зіставлені зі знаннєвими clause, і результат, що напряму йде в `reason` без ручного редагування | ok |
| `my-lisp` | `tests/forward.rs` | 44 | `lib/forward.my`, Кроки 1-7, 10-12, 14 та 15 forward-chaining рушія в стилі CLIPS: `assert-fact!` — розширення глобальної working memory, `fire-rule` — унікація шаблону правила з одним фактом, `fire-rule-on-facts`/`fire-rule-on-working-memory` — застосування одного правила до всього списку фактів, `fire-rules-on-facts`/`fire-rules-on-working-memory` — застосування всього набору правил за один прохід, `run` — цикл до fixpoint, `assert-facts!` — злиття результату `run` назад у `*working-memory*`, `retract-fact`/`retract-fact!` — видалення одного факту через `equal?`, система підтримки істинності з одним обґрунтуванням, повний JTMS із множинними обґрунтуваннями, `fire-rule-multi`/`fire-rules-multi`/`run-multi` — спільна з `lib/reason.my` точна форма правил, (Крок 7) negation as failure, (Крок 10) умови `or`, (Крок 11) умови `and`, (Крок 12) умови `test`, (Крок 14) багатоумовний близнюк JTMS (`run-jtms-multi!`), і (Крок 15) квантифіковані умовні елементи `exists`/`forall` — знайдені відсутніми при імпорті реального зовнішнього CLIPS-файлу (`tests/fixtures/sudoku-external.clp`), той самий клас мовчки-мертвого правила, що й `not`/`or`/`and`/`test`; `exists` успішний на будь-якому збігу своїх під-умов, не зв'язуючи нові змінні назовні (побудований на `match-conditions`, відкидаючи будь-яку під-підстановку, що задовольнила умову); `forall` — після того, як перша спроба через композицію `not`/`and` провалила debug-тест, бо `match-negated-condition` ніколи не рекурсує через `match-one-condition` для складених внутрішніх патернів — написаний напряму: кожна підстановка від першої умови повинна також задовольняти решту | ok |
| `my-lisp` | `tests/narrate.rs` | 7 | `lib/narrate.my`, половина мосту "структура -> текст" (обернена до `lib/understand.my`): `narrate-fact` — точна структурна обернена функція до `understand-is`/`understand-relation` (round-trip через `understand`), `narrate-provenance` — обхід запису `provenance` у пояснення-список слів, з'єднане `because`/`and`, і окремий регресійний тест, що фіксує задокументоване обмеження: нерозв'язані `(var ...)` у власній голові виведеного правила лишаються нерозв'язаними (`provenance` не несе фінальної підстановки запиту) | ok |
| `my-lisp` | `tests/persistent_map.rs` | 8 | `lib/persistent-map.my`, AVL-збалансована персистентна мапа: запити до порожньої мапи, insert-then-get, форма "maybe", що розрізняє збережене значення `()` від відсутності, заміну наявного ключа замість дублювання, персистентність (оригінальне дерево незмінне після побудови нового з нього), in-order `map->list`, що повертає відсортовані ключі, і — сама суть вибору збалансованого дерева — сім ключів, вставлених уже у відсортованому порядку (підручниковий найгірший випадок для незбалансованого дерева), що лишаються на висоті 3 замість виродження в зв'язаний список висотою 7 | ok |
| `my-lisp` | `tests/tcp.rs` | 11 | сирі TCP-примітиви (`tcp-connect`/`tcp-listen`/`tcp-accept`/`tcp-read`/`tcp-write`/`tcp-close`): реальний клієнт/сервер round-trip через два OS-потоки з окремими `Session` (сервер приймає, читає, ехо-ить, закриває; клієнт підключається, пише, читає ехо, закриває), `tcp-read` повертає `""` на закритому з'єднанні замість помилки, підключення до порту, який ніхто не слухає, провалюється названо (`InvalidForm`), не зависає, `tcp-write` повертає свій аргумент-вміст без змін, і структуровані помилки `Type` на нерядковому хості, порту поза межами й нез'єднанному аргументі | ok |
| `my-lisp-literate` | `tests/literate_offsets.rs` | 4 | зіставлення зміщень початкового коду literate-Markdown | ok |
| `my-lisp-wasm` | unit-тест (`src/lib.rs`) | 1 | WASM-адаптер видає ту саму точну/однопрохідну структуру обчислення, що й нативне ядро | ok |
| `my-lisp` | `tests/advice.rs` | 2 | наскрізний `understand → advise → reason-in → narrate-answer`, включно з відхиленням без зміни журналу | ok |
| `my-lisp` | `tests/world.rs` | 54 | `lib/world.my`: незмінна історія й identity плюс усі основні legacy writer-и над World-переходами, включно з atomic rollback і рівно одним обчисленням аргументів guarded-макросів | ok |
| `my-lisp` | `tests/content_store.rs` | 5 | `lib/content-store.my`: незмінне зберігання address→content, отримання, дедуплікація та збереження різних історій World | ok |
| **Разом** | | **405** | | **405 пройдено, 0 провалів, 0 пропущено** |

Незалежна від реалізації conformance-фікстура [`tests/fixtures/conformance.my`](../tests/fixtures/conformance.my) — власний формат і правила описані в [`tests/fixtures/README.md`](../tests/fixtures/README.md) — підключається напряму в `crates/my-lisp/tests/mccarthy.rs` через `include_str!` і перевіряється в межах цього набору, окремо не рахується. Набір також фіксує `symbol?` як функцію `lib/core.my`, а не Rust built-in.

```bash
cargo test --workspace
```

Останній зафіксований запуск: 2026-08-11, Linux x86_64, Rust/Cargo 1.93.0 із Guix — усі проходять, 0 провалів, 0 пропущено.

**Застереження щодо reader вирішено**: reader раніше не мав синтаксису dotted-pair-літералів — `'(p . 0)` парсив `.` як звичайний символ, даючи 3-елементний власний список замість справжньої dotted pair, хоча printer уже друкував справжні dotted pairs саме так. Виправлено 2026-08-09 (`ExprKind::Pair`, див. рядок unit-тестів вище) — quoted `'(p . 0)` і реальний `(cons 'p 0)` тепер `equal?`.

## Deutsch

Dieses Repository hat eine Testebene: die vier Rust-Crates unter `crates/`, ausgeführt mit `cargo test --workspace`. Eine separate JS/Web-Testsuite gibt es hier nicht — der eigenständige Web-REPL (`public/my-lisp-cli-web.html`) wird aus demselben WASM-Crate gebaut, das unten abgedeckt ist, und die frühere Node/Playwright-Suite blieb im `my-idea`-IDE-Repository zurück, aus dem dieser Crate-Satz extrahiert wurde (siehe [`docs/versioning.md`](versioning.md)). Diese Tabelle ist die aktuelle Quelle der Wahrheit und sollte aktualisiert werden, sobald eine Suite Tests gewinnt oder verliert.

| Crate | Suite | Tests | Deckt ab | Ergebnis (letzter Lauf) |
|---|---|---:|---|---|
| `my-lisp` | Unit-Tests (`src/parser.rs`, `src/environment.rs`, `src/eval/mod.rs`, `src/error.rs`, `src/bignum.rs`) | 44 | Reader-/Parser-Grenzfälle, Isolation des lexikalischen Scopes, Single-Pass-Auswertung, Makro-Expansion, zeichenbasierte Zeile/Spalte und "^"-Rendering für strukturierte Fehler, das von Hand geschriebene beliebig genaue `BigInt` (add/sub/mul/div/ggT/Ordnung/Dezimal-Parsing und -Formatierung) hinter `Rational`, die Dotted-Pair-Syntax des Readers (`ExprKind::Pair`) — `(a b . c)` parst als verschachtelte Pairs, ein einzelner `.` zwischen genau zwei Teilausdrücken innerhalb von Klammern, mit sauberen Parse-Fehlern für ein herumhängendes `.` auf beiden Seiten oder zusätzliche Tail-Ausdrücke, während ein nackter `.` außerhalb einer Liste weiterhin als gewöhnliches Symbol gelesen wird — und die Exactness-Regel für Zahlenliterale (`Exactness`): ein Ganzzahlliteral parst beliebig genau und exakt, ein Dezimal- oder Exponentialliteral inexakt, unabhängig von der Anzahl nachgestellter Nullen (`3`/`3.0`/`3.00`/`3e0`) | ok |
| `my-lisp` | `tests/mccarthy.rs` | 85 | die sieben McCarthy-Primitive, exakte/inexakte Arithmetik, Kernvergleiche `<`/`>`/`=` plus nach core.my verschobene `<=`/`>=`, `print`s sitzungsweites Ausgabetranskript (auch durch Closures hindurch), `read`/`eval`, die die Read-Eval-Schleife von Hand schließen, Lambda-Semantik, strukturierte Fehler, `lib/core.my`-Listenwerkzeuge (`length`, `reverse`, `append`, `map`, `filter`, `reduce`), `let`/`let*`, `equal?`, exakte Arithmetik jenseits von `i64` (Fakultät von 30, exakt berechnet), die minimale Symbol-/String-Introspektion für `lib/clips-import.my`, `read-all`, den Dotted-Pair-Reader-Fix, die Synchronisationsprüfung zwischen `my-lisp-constitution.my` und `tests/fixtures/conformance.my`, die Prozess-Absicherung der symbolischen Schicht, und variadische `lambda`/`defmacro`-Parameter (Dotted `(a b . rest)`-Listen und nacktes `args`-Symbol) — die Änderung, die `list` selbst von einer Rust-Sonderform in `lib/core.my` verschob, und optionale Ressourcengrenzen (`Environment::with_cons_limit`/`with_numeric_bit_limit`), die `OutOfMemory`/`NumericOverflow` zu echten, geprüften `ErrorKind`-Kategorien machten, und `write-to-string`-Serialisierung sowie `write-file` (Round-Trip mit `read-file`, Überschreiben-statt-Anhängen-Semantik, strukturierte Fehler bei einem Nicht-String-Pfad/-Inhalt oder falscher Arität), und `string-append`/`string<?` (Verkettung, lexikographische Ordnung, strukturierte Fehler bei Nicht-String-Argumenten oder falscher Arität), und `process-run` (standardmäßig deaktiviert, strukturierte Fehler, wenn die Sitzung nie eine Allowlist aktiviert hat, Ausführung eines explizit erlaubten Programms, Ablehnung eines Programms außerhalb der Allowlist, und Ablehnung von Nicht-String-Programm-/Argumentwerten) | ok |
| `my-lisp` | `tests/stack_safety.rs` | 6 | Tail-Rekursion und Clone/Drop tiefer Listen mit konstantem Rust-Stack, `append`/`filter`/`map`/`length` aus `lib/core.my` bleiben stack-sicher bei einer 100.000-Elemente-Liste | ok |
| `my-lisp-cli` | `tests/cli.rs` | 12 | die kompilierte Binärdatei durchgängig: `--version`/`--help`, Dateiausführung, Exit-Codes bei Parse-/Eval-Fehlern, fehlende Datei, Vorladen von `lib/core.my`, REPL-Verlauf, der über getrennte Sitzungen in `~/.my-lisp-history` erhalten bleibt, `(read)`, das im Dateimodus eine Zeile aus echtem, per Pipe übergebenem stdin liest, und `*argv*` (alles nach dem Dateinamen, als my-lisp-Liste von Strings, für das Versionsargument von `scripts/release.my` — leer, kein Fehler, wenn nichts dem Dateinamen folgt) | ok |
| `my-lisp` | `tests/meta_eval.rs` | 14 | `lib/meta-eval.my`, der metazirkuläre Evaluator: Selbstauswertung, `quote`, Dispatch von Arithmetik-/Listen-Primitiven, `cond`, Lambda-Anwendung (ein- und mehrausdrucksweise Rümpfe), Closures, die freie Variablen aus einer übergebenen env erfassen, Funktionen höherer Ordnung, `def`/`defmacro` via `my-eval-program`, Laden eines wörtlichen Ausschnitts von `lib/core.my` und Ausführung über `my-eval`, die dokumentierte Grenze rekursiver Top-Level-`def`s | ok |
| `my-lisp` | `tests/unify.rs` | 12 | `lib/unify.my`, das Unifikations-Primitiv: Atom-Abgleich, Variablenbindung/-auflösung, strukturelle (zusammengesetzte) Unifikation, Fehlschlag bei Nichtübereinstimmung, transitive Auflösung verketteter Variablen, Unifikation einer Variable mit sich selbst ohne Bindung, vollständiges `apply-subst` einer Anfrage, occurs-check, `thread-conjunction` — der Konjunktions-Durchlaufkern, auf dem sowohl `lib/reason.my` als auch `lib/forward.my` ihren konjunktiven Abgleich aufbauen | ok |
| `my-lisp` | `tests/reason.rs` | 13 | `lib/reason.my`, die Inferenz-Engine: einfache Fakten, rekursive Regeln, Standardizing apart, negation as failure, Beweisbäume und `explain-proof`-Trace-Ausgabe, die Unterscheidung "beweisbar" vs. "nicht beweisbar" in `reason-explain`, `count-usage` — Durchlauf eines Beweisbaums zu einer `(regelkopf . anzahl-verwendungen)`-Tabelle, `provenance` — wandelt einen Beweisbaum-Knoten in einen Datensatz `(statement ziel (source fact\|rule) (rule ...) (derived-from ...))` um | ok |
| `my-lisp` | `tests/knowledge.rs` | 42 | `lib/knowledge.my`, modulare Wissenspakete auf Basis eines append-only `*knowledge-journal*`: geschütztes `advise`, atomares `advise-all` und versionierter reiner `my-lisp-knowledge`-Paketimport/-export, einschließlich Validierung, Rollback bei jeder ungültigen Clause sowie interner und abgeleiteter Widerspruchserkennung über vorgeschlagenes und vorhandenes Wissen; `defmodule`/`tell-knowledge` schreiben `tell`-Ereignisse, `module-clauses-now` projiziert aktuelle Clauses, `retract-knowledge` schreibt `retract`-Ereignisse, `module-known?` unterscheidet unbekannte von leer zurückgenommenen Modulen, `reason-in`/`forward-in`/`describe` lesen isolierte Module, `record-usage!`/`usage-of` sammeln Regel-Nutzung, `forward-in` läuft bis zum Fixpunkt, plus `knowledge/family.my` mit rekursiver `ancestor`-Regel | ok |
| `my-lisp` | `tests/clips_import.rs` | 36 | `lib/clips-import.my`, Schritte 1-5, 8-9, 13, 15-19 (Schritt 5 ersetzt durch Schritt 7 in `lib/forward.my`; der `exists`/`forall`-Guard aus Schritt 19 ersetzt durch Schritt 15 in `lib/forward.my`) eines Importers für alte symbolische KI-Systeme (beginnend mit CLIPS, NASA JSC 1985): wandelt CLIPS-Formen `deffacts`/`defrule` in `defmodule`-fertige Clauses um, `?x`-Syntax, mehrfaktige `assert`-Aufrufe, `deftemplate`s benannte Slots, `printout` disqualifiziert nicht mehr, das Entfernen der optionalen Docstring- und `(declare (salience N))`-Präambel einer `defrule` vor ihren Bedingungen, `defmodule`-qualifizierte deftemplate-Namen, wertlose Multislot-Bedingungen, einen echten Rust-Stack-Overflow bei großen `deffacts`-Blöcken (gefunden an `tests/fixtures/animal-external.clp`), behoben mit tail-rekursivem `-onto`-Akkumulator, eine fünfte/sechste/achte echte externe `.clp`-Datei (`manners`/`dilemma1`/`mab`), die bestehende Policy bei wachsendem Maßstab bestätigten, eine siebte (`zebra`), die den Importer in echtem Maßstab prüfte, und eine neunte (`tests/fixtures/sudoku-external.clp`, CLIPS' eigener Sudoku-Löser), deren Bedingung `(exists (unsolved))` jetzt korrekt importiert und konvertiert wird — `clips-convert-template` hat gelernt, auch in `exists`/`forall`-Unterbedingungen zu rekursieren, dieselbe Lehre, die Schritt 13 bereits für `not`/`or`/`and` gab | ok |
| `my-lisp` | `tests/understand.rs` | 5 | `lib/understand.my`, die Brücke kontrollierter natürlicher Sprache: feste Wortlisten-Formen (`X is a Y`, `X V Y`, `all X have Y`), abgebildet auf Wissens-Clauses, deren Ergebnis direkt ohne manuelle Bearbeitung in `reason` einfließt | ok |
| `my-lisp` | `tests/forward.rs` | 44 | `lib/forward.my`, Schritte 1-7, 10-12, 14 und 15 einer CLIPS-artigen Forward-Chaining-Engine: `assert-fact!` erweitert die globale Working Memory, `fire-rule` unifiziert das Muster einer Regel mit einem Fakt, `fire-rule-on-facts`/`fire-rule-on-working-memory` wenden eine Regel auf eine ganze Faktenliste an, `fire-rules-on-facts`/`fire-rules-on-working-memory` wenden ein ganzes Regelwerk in einem Durchlauf an, `run` läuft bis zum Fixpunkt, `assert-facts!` führt das `run`-Ergebnis in `*working-memory*` zusammen, `retract-fact`/`retract-fact!` entfernen einen Fakt über `equal?`, ein Truth-Maintenance-System mit einer Begründung, ein vollständiges JTMS mit mehreren Begründungen, `fire-rule-multi`/`fire-rules-multi`/`run-multi` teilen sich mit `lib/reason.my` dieselbe Regelform, (Schritt 7) Negation als Fehlschlag, (Schritt 10) `or`-Bedingungen, (Schritt 11) `and`-Bedingungen, (Schritt 12) `test`-Bedingungen, (Schritt 14) ein Mehrbedingungs-Zwilling des JTMS (`run-jtms-multi!`), und (Schritt 15) quantifizierte Bedingungselemente `exists`/`forall` — fehlend entdeckt beim Import einer echten externen CLIPS-Datei (`tests/fixtures/sudoku-external.clp`), dieselbe Bugklasse einer still toten Regel wie `not`/`or`/`and`/`test`; `exists` gelingt bei jedem Treffer seiner Unterbedingungen, ohne neue Variablen nach außen zu binden (aufgebaut auf `match-conditions`, verwirft jede Unter-Substitution, die es erfüllt hat); `forall` — nachdem ein erster Versuch über `not`/`and`-Komposition einen Debug-Test durchfallen ließ, weil `match-negated-condition` nie über `match-one-condition` für zusammengesetzte innere Muster rekursiert — ist direkt geschrieben: jede Substitution, die die erste Bedingung liefert, muss auch die übrigen erfüllen | ok |
| `my-lisp` | `tests/narrate.rs` | 7 | `lib/narrate.my`, die Hälfte "Struktur -> Text" der Brücke (Gegenstück zu `lib/understand.my`): `narrate-fact` als exakte strukturelle Umkehrung von `understand-is`/`understand-relation` (Round-Trip durch `understand`), `narrate-provenance` wandelt einen `provenance`-Datensatz in eine mit `because`/`and` verbundene Worterklärung um, und ein dedizierter Regressionstest, der die dokumentierte Einschränkung festhält: ungelöste `(var ...)` im eigenen Kopf einer abgeleiteten Regel bleiben ungelöst (`provenance` trägt nicht die endgültige Substitution der Anfrage) | ok |
| `my-lisp` | `tests/persistent_map.rs` | 8 | `lib/persistent-map.my`, die AVL-balancierte persistente Map: Nachschlagen in der leeren Map, Insert-dann-Get, die "Maybe"-Form, die einen gespeicherten `()`-Wert von Abwesenheit unterscheidet, Ersetzen statt Duplizieren eines vorhandenen Schlüssels, Persistenz (der ursprüngliche Baum bleibt unverändert, nachdem ein neuer daraus gebaut wurde), In-Order-`map->list`, das sortierte Schlüssel zurückgibt, und — der eigentliche Punkt der Wahl eines balancierten Baums — sieben bereits sortiert eingefügte Schlüssel (der Lehrbuch-Worst-Case für einen unbalancierten Baum), die bei Höhe 3 bleiben statt zu einer verketteten Liste der Höhe 7 zu entarten | ok |
| `my-lisp` | `tests/tcp.rs` | 11 | die rohen TCP-Socket-Primitive (`tcp-connect`/`tcp-listen`/`tcp-accept`/`tcp-read`/`tcp-write`/`tcp-close`): ein echter Client/Server-Round-Trip über zwei OS-Threads mit getrennten `Session`s (Server akzeptiert, liest, echot, schließt; Client verbindet, schreibt, liest das Echo, schließt), `tcp-read` gibt bei geschlossener Verbindung `""` zurück statt einen Fehler, Verbinden zu einem Port, auf dem nichts lauscht, schlägt benannt fehl (`InvalidForm`) statt zu hängen, `tcp-write` gibt sein Inhaltsargument unverändert zurück, und strukturierte `Type`-Fehler bei einem Nicht-String-Host, einem Port außerhalb des Bereichs und einem Nicht-Verbindungs-Argument | ok |
| `my-lisp-literate` | `tests/literate_offsets.rs` | 4 | Offset-Zuordnung von literate-Markdown-Quellcode | ok |
| `my-lisp-wasm` | Unit-Test (`src/lib.rs`) | 1 | der WASM-Adapter liefert dieselbe exakte/Single-Pass-Auswertungsstruktur wie der native Kern | ok |
| `my-lisp` | `tests/advice.rs` | 2 | durchgängig `understand → advise → reason-in → narrate-answer`, einschließlich Ablehnung ohne Journaländerung | ok |
| `my-lisp` | `tests/world.rs` | 54 | `lib/world.my`: unveränderliche Geschichte und Identität plus alle wesentlichen Legacy-Writer über World-Übergängen, einschließlich atomarem Rollback und genau einmaliger Auswertung geschützter Makroargumente | ok |
| `my-lisp` | `tests/content_store.rs` | 5 | `lib/content-store.my`: unveränderliche Adresse→Inhalt-Speicherung, Abruf, Deduplizierung und Erhalt verschiedener World-Geschichten | ok |
| **Gesamt** | | **405** | | **405 bestanden, 0 fehlgeschlagen, 0 übersprungen** |

Die implementierungsunabhängige Konformitäts-Fixture [`tests/fixtures/conformance.my`](../tests/fixtures/conformance.my) — eigenes Format und eigene Regeln stehen in [`tests/fixtures/README.md`](../tests/fixtures/README.md) — wird direkt über `include_str!` in `crates/my-lisp/tests/mccarthy.rs` eingebunden und im Rahmen dieser Suite geprüft, nicht separat gezählt. Die Suite fixiert außerdem `symbol?` als Funktion aus `lib/core.my` statt als Rust-Builtin.

```bash
cargo test --workspace
```

Letzter erfasster Lauf: 11.08.2026, Linux x86_64, Rust/Cargo 1.93.0 aus Guix — alle bestanden, 0 fehlgeschlagen, 0 übersprungen.

**Reader-Falle behoben**: Der Reader kannte zuvor keine Syntax für Dotted-Pair-Literale — `'(p . 0)` parste `.` als gewöhnliches Symbol und erzeugte eine dreielementige echte Liste statt eines echten Dotted Pair, obwohl der Printer echte Dotted Pairs bereits genau so ausgab. Behoben am 2026-08-09 (`ExprKind::Pair`, siehe Unit-Test-Zeile oben) — ein quotiertes `'(p . 0)` und ein tatsächliches `(cons 'p 0)` sind jetzt `equal?`.
