; Research-only claim boundary for #432.
; This file is data/prose in Lisp syntax, not semantic authority.

(canon-sculpt-relative-claim
  (scope . "declared basis B = Canon 0 + quote/atom/eq/cons/cond + ordinary lambda/define composition; car/cdr and hidden pair eliminators excluded")
  (strongest-allowed . "within B, no rule creates a top-level fresh runtime payload atom from inside pair data; Church-pair application route is separately falsifiable")
  (not-claimed . "CAR/CDR are absolutely irreducible against every possible future basis")
  (basis-compression-note . "a future single pair-eliminator replacing both projectors is a separate basis-compression hypothesis, not a derivation from B"))
