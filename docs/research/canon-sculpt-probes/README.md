# Canon sculpture research probes

These files are executable falsification probes for issue #419. They are not semantic fixtures and they are not evidence records.

A predicted failure is not evidence. Only after a real run may its observed result be recorded under the repository's `evidence/` protocol. The verification-only child PR that runs these probes must never merge.
